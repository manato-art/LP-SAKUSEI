# 実データを含むローカル専用ファイル（.gitignore と二重で遮断）
.gate-names.local
.scrub.local.json
.touch-scope.local
scrub-map.json
.env
.env.*
node_modules/
dist/
.git/

# ── アップロード削減 ──
# 実行時に要らない採取物は上げない（リポジトリには残す）。
capture/clean/**/dom.html
capture/clean/**/*.json
capture/capture-plan.json
# 画面別cssomは統合版(_merged)に集約済み。個別cssomはアップロードしない。
capture/clean/**/cssom.css
!capture/clean/_merged/cssom.css
# 採取アセット（画像/フォント）は必要
