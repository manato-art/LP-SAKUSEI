/**
 * 「ボットは含めていません」の文言（2026-09-16・本人の依頼）。
 *
 * レポート画面・ダッシュボード・定期レポートで同じ言い方にするため、ここ1か所に置く。
 * 件数を添えるのは、本当に除けているのかを確かめられるようにするため。
 */

/** 何をボットとして除いているか（画面ではマウスを乗せると出る） */
export const BOT_NOTE_DETAIL =
  '検索エンジンの巡回、SNS・チャットのリンクプレビュー、自動操作のブラウザ（計測ツールなど）は数字に入れていません。' +
  'LINE・Instagram・Facebook・X・TikTokなどのアプリ内ブラウザは、人として数えています。'

/** 画面と定期レポートに出す1行 */
export function botNoteText(count: number): string {
  // 0件でも「含めていません」とは書く（書かないと「判定していない」と区別がつかない）
  return count === 0
    ? 'ボットのアクセスは含めていません（この期間に除いたものはありません）'
    : `ボットのアクセスは含めていません（この期間に除いた数：${count.toLocaleString('ja-JP')}件）`
}
