# SquadBeyond 完全再現クローン 企画書

— 全ページ・全状態を本物そっくりに再現する社内非公開クローン

- 版: **v2.0**（v1.0 を多角監査してブラッシュアップ。旧版は `企画書_v1.0_archive.md`）
- 作成日: 2026-08-31
- 作成: 企画担当AI（フェイブル）
- 想定読者: 実装担当AI（本会話を一切見ていない前提。この文書だけで実装できるように書いてある）
- 再現対象: `https://app.squadbeyond.com/`（御社子会社が制作したLP A/Bテスト運用SaaS「Squad beyond」）

---

## 0. この企画書の読み方（実装AIへの絶対前提）

- **あなた（実装AI）は、この文書に書かれていること以外の会話履歴を持っていない前提で書かれている。** 不明点を「たぶんこうだろう」で埋めてはいけない。曖昧な箇所に当たったら手を止め、§16「未確定事項」を見て、選択肢（推奨1つ明示）を添えて人間に確認する。
- **この案件は「再現」であって「改善」ではない。** 見た目・挙動・遷移はオリジナルに忠実であることが最優先。あなたの美的判断でUIを「良く」しない。社内の一般的UI規約（余白ゼロ・絵文字禁止・カード型メニュー等）は新規プロダクト向けであり、本案件では忠実再現がそれらに優先する（§12で明記）。
- 章立ては「先に固定すべきこと（目的・スコープ・用語・最大リスク）→ 実態 → 中核方式 → 母集合（ルート×状態）→ 難所 → データ/モック → アーキ → 規約 → 受け入れ → 計画」の順。**上から順に確定させながら進める。**

---

## 1. 目的とスコープ

### 1-1. この企画書の単一目的
- 本案件の目的は、SquadBeyond（app.squadbeyond.com）の**全ページ・全状態が、本物そっくりに触って動く「完全再現クローン」を作ること、ただそれだけ**である。
- **範囲外（作らない・目指さない）**: バグの発見・再現・可視化、意図的な不具合の作り込み、デバッグ用の計装や注入口、開発者向けサンドボックス/実験環境化 —— これらは一切目的に含めない。将来もこの文書のスコープに入れない。（この段落は恒久ガード。以降どの追記でもこの範囲外を侵さないこと。）
- バックエンドの集計ロジック（売上/ROAS/粗利の計算）・DB・サーバー内部実装は**再現対象外**（モックが固定値/擬似計算で返す）。ただしこれは「内部実装まで一致させない」という意味であり、**見た目と挙動の忠実再現は最優先で守る**。表示の内部整合のための決定論的な擬似生成規則（§10-5）は持つ（＝「サーバー集計は再現しないが、表示が矛盾しない擬似生成はする」）。

### 1-2. 「完全再現達成」の定義（この案件のゴール判定）
次のすべてが**機械検証で緑**になり、単一のトレーサビリティ台帳（§8）の上で「**対象内(in-scope)状態＝100%合格 / 対象外(残差)＝明示的に列挙・承認済み**」となっている状態を「完全再現達成」とする。単なるハッピーパス82枚では達成としない。

1. **母集合の確定と網羅証明**: ルート一覧をアプリ自身のルートテーブル・ナビ全巡回・sitemapと突合し「未知ルート0」を証明する（§6-1）。全ルートを「route → 実画面 → 内部状態（一意ID）」の台帳に展開済み。82という数はこの証明の後に確定値として扱う。
2. **全状態がレンダされる**: 各（ルート×状態）が、ローカルモックから HTTP200・`#root`非空・console error 0・本番/外部ホストへの出ネットワーク0 で実際に表示できる（空/読込/エラー/権限拒否/主要モーダル/確認ダイアログ/トースト/PC・SP/ウィザード各ステップ/一覧端 を含む）。
3. **視覚忠実度**: 各状態が匿名化済みキャプチャのベースラインに対しピクセル/SSIM閾値内で一致（動的・ユーザーデータ領域はマスク）。土台は採取した実CSS（約386KB・Emotionハッシュごとverbatim）＋自己ホスト化フォント/アイコンを使用。
4. **インタラクション/ナビ忠実度**: ナビゲーショングラフを全走行可能（全リンク/ボタンが正しい再現ルートへ到達し、404や空ガワの行き止まりが無い）。タブ/ソート/ページ送り/フィルタ/検索/フォーム＋バリデーション/CV速報がモック経由で見分けのつかない挙動を返す。
5. **データ整合**: 一覧↔詳細↔レポートの数値が内部整合し、固定seedでリロード・再ナビ後も決定論的に一致。派生KPI（粗利/ROAS/ROI/CVR/CPA）が導出規則（§10-5）を満たす。
6. **安全性**: リポジトリに実PII/秘密情報が無い（匿名化ゲート合格・生キャプチャは隔離しコミットしない）、本番API接触0。
7. **残差の明示的境界と承認**: 下記は「ビット一致の対象外」と宣言し、到達可能な天井を明記する（§9-1・§13で列挙）。この対象外リストが箇条化され承認されていること。
   - LPエディタの1操作1操作のWYSIWYG同一性（到達天井＝ガワ完全一致＋実LPプレビュー＋動作する独自編集レイヤー）
   - サーバー集計値の正確性（到達天井＝内部整合した擬似生成値）
   - ヒートマップの密度算出アルゴリズム（到達天井＝合成オーバーレイ）
   - 認可アカウントから観測不能な状態（§5-7）

### 1-3. スコープ表
| 項目 | 内容 |
|---|---|
| 再現方式 | ブラックボックス（ソース入手不可）。デベロッパーツール＋Claude in Chrome拡張で実サイトを機械捕捉して作り直す |
| 対象範囲 | 全ページ（全82ルート）×全状態を、触って動く状態で再現 |
| 技術ベース | HTMLベース（ブラウザで開ける実HTML/CSS/JS）を Claude Code（CLI）で構築 |
| 中核方式 | **capture-and-rehydrate**（実HTML/CSS/DOMを採取→匿名化→忠実な土台にし、挙動だけモックへ再配線。§5・§11） |
| 作らないもの（範囲外） | バックエンドの集計ロジック・DB・サーバー内部実装は再現しない（モックが固定値/擬似計算で返す）。**完全再現以外の用途（バグ発見・デバッグ・サンドボックス化・意図的な不具合作り込み等）は本案件の範囲外** |
| 公開範囲 | 社内限定・**非公開**。GitHubにあげる場合も private。子会社プロダクトの複製物なので外部公開・再配布しない |

### 1-4. 再現の基準状態＝「新規アカウント（空）」・実際に使えること
- 再現の基準状態は、**新規アカウントを発行した直後の"まっさら"な状態**とする。現在の本番アカウントに入っている実データ（多数のbeyondページ・実施策・実数値）は**この案件では再現しない**。それらは後日あらためて取り込む（本案件のスコープ外）。
- ただし成果物は静的な空画面ではなく、**「実際に使える」**こと。新規アカウントとして入り、フォルダ作成 → beyondページ作成 → Version追加 → LP編集 → 公開、といった操作が**その場で反映され、セッション内で永続する**（モックが状態を保持。§10-9）。
- したがって各ルートの最重要状態は **empty（新規＝0件）＋ creation flow（作成→反映）**。populated（データ充填）状態の再現は後日の実データ取込フェーズへ回す。これに伴い**採取の主対象も「シェル＋各ページの空状態＋作成フロー＋コンポーネントの見た目」へ絞られ、採取量は大幅に減る**（§5・§6-4）。
- 「完全再現達成」（§1-2）の判定母集合も、この基準状態（空＋作成フロー＋主要インタラクション）に対して適用する。populated状態の忠実度は後日フェーズの受け入れとする。

---

## 2. 用語定義（正準語・一意固定）

実装AIはUI文言・ルート・データモデルで下記の正準語を機械的に統一して使う。

| 概念 | 表示名（UI文言） | 内部識別子 / モデル名 | ルート |
|---|---|---|---|
| A/Bテストの器（1つのLP施策の単位） | **beyondページ** | `ab_test` / `AbTest` | `/ab_tests/:ab_test_uid/...` |
| beyondページ内のコンテンツページ | 記事 | `article` / `Article` | — |
| 記事のバリアント（A/B比較される版・エディタ編集対象） | **Version** | `version` / `Version` | — |
| beyondページを束ねる整理単位 | フォルダ | `folder` / `Folder` | `/folders/...` |
| 流入元（Facebook, LINE, TikTok等） | 媒体 | `media` / `Media` | — |
| コンバージョン | CV | `conversion` / `Conversion` | `/conversions` |

> 補足: `/ab_tests/:uid/articles` は「記事一覧」ではなく**LPエディタ本体**（§6-2・§9-1）。以降この誤解を持ち込まない。

---

## 3. Step 0 ―― 最大リスク（着手前に必ず読む）

capture方式では**生キャプチャに機密が丸ごと入る**ため、v1.0より匿名化を最重要リスクに格上げしている。

### 3-1. 【最大リスク①】本番の実データを絶対にクローンへ持ち込まない
オリジナルは親会社グループの**機密情報の塊**（実際の売上・配信金額・粗利・ROAS、実在するLP施策名、**実在従業員の氏名**、独自ドメイン、uid/トークン等）。

- クローン成果物・コミット対象に一切の実データを入れない。すべて**架空の合成データ**に置換する（§5-5の匿名化パイプライン）。
- 採取してよいのは「データの構造（フィールド名・型・階層）」と「見た目（レイアウト・CSS・DOM構成）」だけ。**値そのものは架空へ置換**する。

### 3-2. 【最大リスク②】クローンを本番APIに絶対に接続しない
`api.squadbeyond.com` / `api-workers.squadbeyond.com` / `report.squadbeyond.com` / 本番 `/cable` へ実リクエストを飛ばすコードを一行も書かない。データは§10のローカルモックからのみ供給。APIベースURLは `http://localhost:xxxx` に固定し、本番ドメインをコードにハードコードしない（§13で機械検査）。

### 3-3. 【最大リスク③】生キャプチャの隔離（コミット禁止）
生キャプチャ（実DOM/実CSS/実APIレスポンス）は**リポジトリの外**の隔離ディレクトリ `~/squadbeyond-capture-quarantine/` に置き、**絶対にコミットしない**。土台化・コミットしてよいのは匿名化通過後の成果物のみ（§5-5）。

### 3-4. 【姿勢】権利・セキュリティの明示スタンス
商用SaaSのCSS/DOM/HTMLをverbatim複製し、ログイン済みセッションを駆動して採取するため、以下を明示スタンスとして守る。
- **社内限定・非公開・再配布しない。** 自己の**認可されたアカウント**からのみ採取する。
- セッションの資格情報（Cookie/トークン）は成果物・ログに保存しない。採取は本人ログイン済みChrome＋Claude in Chrome拡張の範囲で行う。

### 3-5. 忠実再現 > 社内UI美観規約
「余白ゼロ・エッジtoエッジ」「絵文字禁止→SVGアイコン」「スマホはカード型メニュー」等の社内UI規約は新規プロダクト向け。本案件はオリジナル忠実再現が最優先で、オリジナルがそうでない限り適用しない（＝勝手にUIを"改善"しない）。

---

## 4. 再現対象の実態（2026-08-31 実測）

### 4-1. フロントエンド
| 項目 | 実測値 |
|---|---|
| 種別 | React 製 SPA。React Router（`__reactRouterVersion` 確認）、Vite ビルド |
| メインJSバンドル | 約 **5.8MB**（難読化。中身は読めない＝ブラックボックスの根拠。クローンでは**使わない・除去する**） |
| スタイル | **Emotion（CSS-in-JS）**。クラスは `css-1v797yu` 等の動的ハッシュ。実CSSは同一オリジンで丸ごと取得可（§5） |
| チャート | **Recharts**（`.recharts-wrapper` 確認・SVG描画） |
| 日付 | **Litepicker**（`--litepicker-*` CSS変数） |
| アイコン | Material Icons（Googleフォント）＋ Font Awesome 5.15.4 |
| フォント | `"Hiragino Sans", sans-serif`、`<html lang="ja">` |
| リアルタイム | ActionCable（WebSocket `/cable`）。CV速報に使用 |

### 4-2. API構成
| ドメイン | 役割 |
|---|---|
| `api.squadbeyond.com/api/v1/*` | メインREST API。Rails/Devise系。共通クエリ `?per_page=&locale=ja&referer=` |
| `api-workers.squadbeyond.com/api/v1/*` | 重い集計系（`teams/versions`, `teams/version-rankings` 等） |
| `report.squadbeyond.com` | ランキングの別ドメイン（サイドバー「ランキング」の外部リンク先） |
| WebSocket `/cable` | ActionCable。リアルタイム更新 |

### 4-3. デザイントークン（実測。§5で実CSSを取り込むので下記は照合用の基準値）
| トークン | 値 |
|---|---|
| ブランド/アクセント（プライマリ） | `#0091FF`（rgb 0,145,255） |
| プライマリ濃色（hover等） | `#0074CC` |
| ページ背景 | `#ECECEC` |
| 中立ボタン/薄面 | `#F4F4F4` |
| テキスト主/副 | `#000000` / `#151515` / `#808080`（副） |
| カード面 | `#FFFFFF` |

### 4-4. 外部計測/監視タグ（クローンには一切入れない・一意リスト）
再現対象が読み込む外部SaaS: **Channel.io / Pendo / HubSpot / Google Tag Manager / GA4 / Mixpanel / Sentry**。
- クローンではこれらを**一切ロードしない**（本番計測への混入防止・不要）。UI上でチャットバブル等の"見た目"だけ要るものは静的ダミーで置く（§9-8）。
- Sentry は「再現対象が使う技術」であると同時に「クローンには入れないSaaS」でもある。扱いは**本リスト（入れない）に一本化**する。

### 4-5. 実CSSの捕捉可能性（実測・§5の前提）
アプリCSSは同一オリジンで全てHTTP200のまま丸ごとfetch可能: `normalize.css`(約8KB) / `/assets/index-cb391eb6.css`(**約322KB=メイン**) / `/assets/index-05deeb9d.css`(約51KB) / アイコンCSS(約4KB) 他、計**約386KB**。LPプレビューは**同一オリジンiframe**でDOMアクセス可（実測: iframe内 htmlLen 約20万字・styleタグ25・linkタグ42）。→ 実CSS・実DOMを土台化すれば視覚忠実度をほぼ完璧化できる。

---

## 5. 再現の中核方式：capture-and-rehydrate と自動捕捉ハーネス

**「computed styleを目視で1個ずつ再構築する」旧方式は採らない。** 同一オリジンで全CSS・全ルートのレンダDOM・LPプレビューiframe・APIレスポンスが機械採取できる（§4-5）ため、**実サイトの実HTML/実CSS/実アセットをそのまま忠実な土台(substrate)に据え、挙動だけをローカルモックへ再配線する**。目視再構築は捕捉不能な箇所のみの最終手段。

### 5-1. 全体フロー（4工程）
1. **Capture（機械採取）**: ログイン済みChrome＋Claude in Chrome拡張で、各ルート×各状態の「レンダ済み `outerHTML`／主要要素のcomputed style／APIレスポンス(HAR＋個別JSON)／フルページスクショ」を採る。全ページ共通CSS一式・フォント/アイコン/画像アセットは1回だけ丸ごと落とす。→ すべて**隔離ディレクトリ**（`~/squadbeyond-capture-quarantine/`・コミット禁止）へ。
2. **Sanitize（匿名化）**: 生キャプチャを決定論置換辞書で架空値へ一括置換し、外部タグ・5.8MBバンドルを除去（§5-5）。grepゲート合格まで次工程へ通さない。
3. **Rehydrate（土台化＋再配線）**: 匿名化済みの実HTML/CSSを各ページの土台として配置。挙動（タブ/ソート/ページネーション/モーダル/フォーム/エディタ操作）は薄いローカルTSで最小再実装し、データ取得はモックAPI（§10・localhost固定）へ向ける。
4. **Verify（忠実度検証）**: 実サイトのベースラインとクローンを同一ビューポートで撮り、動的領域をマスクしてピクセル差分＋DOM構造差分で機械判定（§13）。

### 5-2. 環境分離とハンドオフ（採取係と実装係）
capture方式は二層構造になる。**曖昧にすると実装が採取に暗黙依存して詰まる**ため明記する。
- **採取係**（ログイン済みChrome＋Claude in Chrome拡張を持つ主体）が §5-1[1] を実行し、匿名化（§5-5）まで行う。生キャプチャは採取係の隔離ディレクトリから出さない。
- **実装係**（Claude Code CLI）は**ライブ接続を持たず**、匿名化済み成果物（`capture/clean/` の DOM/CSS/HAR/fixtures/screenshot/manifest）だけを受け取って組み立てる。
- ハンドオフ物は Git で版管理（`capture/clean/` はコミット可）。採取ビルドのハッシュを `docs/capture-manifest.md` に記録し、全採取物のビルドを一致させる。

### 5-3. ディレクトリと命名規約
```
~/squadbeyond-capture-quarantine/   ← 生キャプチャ（リポジトリ外・コミット禁止）
squadbeyond-clone/
├── capture/
│   ├── manifest.json              # 全捕捉物の索引(route,state,取得時刻,元URL,sha256,採取ビルドhash)
│   ├── css/                       # 同一オリジン実CSS一式(全ページ共通・1回だけ取得・匿名化後)
│   │   normalize.css / index-main.css(322KB) / index-sub.css(51KB) / icons.css
│   ├── assets/ fonts/ icons/ img/ # woff2・アイコン・画像をローカル退避(自己ホスト用)
│   ├── clean/<route-slug>/<state>/# 匿名化後(コミット可・実際の土台): dom.html / iframe.html / computed.json / screenshot.png / network.har / fixtures/*.json
│   └── diff/<route-slug>/<state>/ # 検証時の差分PNG
├── src/{shared,styles,pages,lib}/
├── mock-server/{index.ts,ws.ts,fixtures/}
├── docs/{routes.md, traceability.md, capture-manifest.md, states/, pages/, api/}
└── package.json                   # dev / mock / verify / lint / test
```
- `route-slug`: パスをファイル名安全化。パラメータは固定ダミーへ（例 `/ab_tests/:uid/articles` → `ab_tests__UID__articles`、uidは常に `UID_0001` 等）。
- `state`: `default / empty / loading / error / modal-open / tab-<name> / sort-<key> / page-2` 等（§7の状態軸）。
- `fixture名`: `<METHOD>__<path(スラッシュを_)>__<queryのsha1先頭8桁>.json`（例 `GET__api_v1_ab_tests__a1b2c3d4.json`）。

### 5-4. capture-plan（機械可読な採取計画）
`capture/capture-plan.json` に、ルートごとに `{route, url, states[], 各stateへの到達操作(interactions[]), 期待エンドポイント[], capture_iframe}` を定義。採取係の拡張はこの計画を上から実行し、各stateで dom/computed/har/screenshot を保存する。例:
```json
{ "route": "/ab_tests/:uid/articles", "slug": "ab_tests__UID__articles",
  "states": [ { "name": "default", "interactions": [] },
    { "name": "tab-version", "interactions": ["click:左レールVersion"] },
    { "name": "code-editor-open", "interactions": ["click:右レール コード編集"] } ],
  "capture_iframe": true }
```

### 5-5. 匿名化（Scrub）パイプライン ―― 丸取り方式の絶対前提
決定論置換で行い、**同じ実値は常に同じ架空値へ写す**（参照整合を保つ）。写像は `scrub-map.json`（**非コミット**）に保持。

- 人名 → 「テスト太郎」「サンプル花子」等（実名は正規表現＋既知社員名リストで検出）
- 金額・数値（売上/配信金額/粗利/ROAS/ROI/PV/CV等）→ ランダムなダミー（桁感は保つ）
- LP施策名・beyondページ名・記事メモ → 「サンプル施策001」等
- ドメイン/URL → `*.squadbeyond.com` を `localhost`/`example.test` へ、絶対URLはローカルパスへ、`/cable` は localhost へ
- uid/トークン/APIキー/メール/電話 → 新規生成のダミー（uidは `UID_0001` 系で一貫）
- 除去 → 本番JSバンドル参照、外部SaaSタグ、Sentry DSN、inlineイベントの本番URL、meta等の個人情報

**保持/置換の境界（重要・過剰scrub防止）**: 本アプリは日本語UI（locale=ja）で、忠実度の本体は**ラベル・ボタン文言・空状態文・エラー文・バリデーション文言といったマイクロコピー**そのもの。これらは**verbatim保持**する。置換するのは**ユーザー生成コンテンツ**（施策名・人名・金額・ドメイン・アップロード物）のみ。保持リスト（画面クローム/i18n文字列）と置換リスト（ユーザーデータ）を `docs/scrub-policy.md` に明文化する。

**grepゲート（通過必須・§13-Eに連動）**: `capture/clean/` と `src/` に対し、(1) `*.squadbeyond.com` が0件、(2) 既知実名リストが0件、(3) 本番uid/トークン形式が0件 を機械チェックし、1件でも残れば土台化・コミットを止める。

### 5-6. Rehydrate（土台化＋再配線）
- `capture/clean/<slug>/<state>/dom.html` を当該ページの初期HTMLとして採用。本番バンドル・外部SaaSタグは除去済み。
- 挙動は薄いローカルTSで最小再実装。データは匿名化済み `fixtures/` を返すモック（§10・localhost固定）へ。
- LPプレビューは iframe に `iframe.html`（匿名化済み）を **srcdoc/動的書き込み**で流し込み、実測どおり動的書き込み型（srcなし）を再現。
- 捕捉不能な箇所（サーバ集計・権限分岐）だけ擬似挙動で埋め、`docs/pages/*.md` に「推測」と明記。

### 5-7. 観測フロア（単一ログイン済みアカウントの限界と代替）
capture-first方式は**そのアカウントが実際に置かれている状態しか観測できない**。他ロールのビュー、権限差分画面（自分が管理者なら非管理者ビューは撮れない/逆も）、実データが在るアカウントでの空状態、空アカウントでの充填状態などは、ライブから原理的に生成できない場合がある。
- 観測不能な状態は §1-2[7] の**残差**として扱い、代替で埋める: (a) 第2アカウント/ロール切替が可能ならそれで採取、(b) 不能なら **CSS＋データモデルから手構築**（＝実CSSを当てた空DOMを合成）し、`docs/pages/*.md` に「手構築/推測」と明記。
- 「全状態を機械採取できる」と過信しない。到達できない状態を正直に列挙するのが完全再現の一部。

### 5-8. 忠実度検証（フィデリティ・ゲート）→ 詳細は §13。

---

## 6. 全82ルートのインベントリと再現優先度

### 6-1. 母集合の網羅証明（「未知ルート0」を先に証明）
82という数を確定値にする前に、次を実施して「未知ルート0」を証明する。
1. JSバンドルの `path:"..."` 抽出（本企画書はこの方法で82件取得済＝付録A）。
2. ログイン済みで**サイドバー/ナビ/リンクを全巡回**し、到達したURLを付録Aと突合。
3. ディープリンク/特定状態/特定ロールからしか出ないルートが無いか確認。
差分が出たら付録Aと `docs/routes.md` を更新してから着手する（母集合が誤れば全下流が誤る）。

### 6-2. ルート → 実画面 突合（ルート≠画面の是正）
抽出ルートは「そのURLのUIの実体」を必ずしも表さない。着手前に全ルートで「宣言ルート → 実際にレンダされる画面 → 内部パネル/状態」を突合し、`docs/routes.md` の各行に注記する。**判明済みの最重要是正**:

| 宣言ルート | v1.0の想定 | 実測された実体 | 対応 |
|---|---|---|---|
| `/ab_tests/:ab_test_uid/articles` | 記事/バージョン一覧 | **【LPエディタ本体＝アプリ中核画面】** iframeベースWYSIWYGビルダー。左レール=基本情報/Version/ポップアップ/レポート、中央=LPプレビュー(PC枠 約620×486・SP枠 約430×640 の iframe・srcなしで動的書き込み・同一オリジンでDOM可)、右レール=画像/テキスト/ブロック追加・履歴・公開・コード編集・undo/redo、左Versionパネル=各Version名+配信割合(数値入力)+更新+Version追加、ページ状態バッジ(準備中等) | このルートを最難関エディタ(§9-1)としてTier2先頭で扱う |
| `/folders/:folder_uid/ab_tests/:ab_test_uid/edit` | LP/HTMLエディタ | 上記articlesがエディタ本体である以上、本ルートとの関係(別入口/別モード/旧版)は未確定 | 観測で両者を突合し確定。**同一のエディタ実装を共有**（2ルートで1コンポーネント）。未確定点は§16 |

> 上記以外にも、一覧を期待したルートが詳細/エディタだったり、`:uid` 有無で別画面のことがある。P1で全ルートを一度ずつ開き、この突合表を埋めてから各ルートの状態インベントリ（§7）を起こす。

### 6-3. サイドバー ⇔ ルート対応
| サイドバー | 主ルート |
|---|---|
| ダッシュボード | `/dashboard`, `/dashboard/:uid` |
| AI | `/sb_ai`, `/sb_ai/chat`, `/sb_ai/chat/:conversationId` |
| タスク | `/tasks` |
| ページ | `/folders`（フォルダ木＋beyondページ）ほかフォルダ/ab_test系 |
| CV速報 | `/conversions`（ActionCableでリアルタイム） |
| ツール | `/articles/bulk_replaces` 等 |
| 外部連携 | `/teams/ad_accounts`, `/teams/asp_accounts`, `/redirections/ad_accounts/*` |
| ドメイン | `/teams/domains` |
| 拡張機能 | `/addon/option-list` |
| レポート除外 | `/report-exclusions` |
| イベント・セミナー | `/seminar` |
| ランキング | 外部 `report.squadbeyond.com`（別アプリ。ローカルにミラー） |

### 6-4. 再現優先度（Tier）
- **Tier 1（土台）**: 共通レイアウト（サイドバー＋ヘッダ）、`/`、`/dashboard`（Recharts＋KPI＋一覧）、`/folders`、認証系（`/users/sign_up` 他）。
- **Tier 2（中核）**: **`/ab_tests/:uid/articles`＝LPエディタ本体（最優先）**、記事/Version/レポート/スプリット設定/離脱ポップアップ/リダイレクト/プレビュー/ヒートマップ/CV速報/タスク。
- **Tier 3（設定・チーム・連携）**: フォルダ配下、チーム、広告OAuthコールバック、審査、通知設定、ユーザー、AI、課金、一括系。
- **Tier 4（管理者・周辺）**: `/admin/*`、`/permissions`、`/introductions`、`/seminar`、`/terms`、`/cable`(WS)、`*`(404)。
> **採取の主対象は空状態＋作成フロー（§1-4）**。基準状態が新規アカウント（空）なので、各ルートで最優先に再現・採取するのは empty 状態と creation flow（作成→反映）。populated 状態は後日の実データ取込フェーズへ回し、採取量を絞る。
> 完全な機械可読ルート一覧は**付録A**（82件）。進捗は §8 の台帳で管理する。

---

## 7. 状態インベントリ（ルート×状態 ＝ 完成単位）

本案件の完成単位は「82ルート」ではなく「**ルート×状態**」。真の画面数はルート数の4〜8倍（想定300〜600状態）。これを漏れなく列挙・採取・追跡するため、状態インベントリを土台（P1）で先に用意する。

### 7-1. 状態軸（全ルート共通の語彙・正本は `docs/states/_taxonomy.md`）
該当しない軸は書かない。
- **表示**: normal(データあり) / empty(0件) / loading(スケルトン・スピナー) / error(取得失敗) / partial-error
- **オーバーレイ**: modal / drawer / confirm-dialog(削除・公開・解約等の破壊操作) / dropdown / context-menu / tooltip / popover / toast(success|error|info) / notification-center
- **ウィザード**: step1..stepN / step-validation-error
- **入力**: form-empty / form-typing / form-invalid / submitting(送信中disabled) / submit-success / submit-error
- **一覧端**: page-first / page-last / page-single / sort-asc / sort-desc / filter-empty
- **権限**: admin / team-owner / member / viewer / access-denied
- **レスポンシブ**: pc / tablet / sp（エディタのLPプレビューは preview-pc(約620×486) / preview-sp(約430×640)）
- **リアルタイム**: cv-toast / cable-connecting / cable-disconnected / cable-reconnecting
- **選択**: tab-<name> / row-selected / bulk-selected / accordion-open|closed
- **非視覚（主要遷移とキーボード到達性のみ対象）**: 主要トランジション演出／スピナー最小表示／フォーカス順・タブ移動・キーボード操作。ビット単位のアニメ一致は非目標だが、主要遷移とキーボード到達性は再現/検証対象。
- **出力面（画面外）**: CSVエクスポート / PDF・印刷ビュー / 送信メールテンプレート。存在するものは「ガワ/挙動の再現要否」を判断して台帳に載せる（省略する場合も明示）。

### 7-2. 状態ID 命名規約
`<route-slug>__<軸>-<値>` の1トークン（例: `dashboard__empty` / `ab_tests-articles-editor__preview-sp` / `folders__delete-confirm` / `conversions__cable-disconnected`）。ルート横断で全ルートに出うる状態は `global__` 接頭辞で `docs/states/_global.md` に置き、各ルートからは参照のみ（重複記述しない）。横断状態: トースト各種／確認ダイアログ（破壊操作）／通知センター／チーム切替ドロップダウン／フォルダ木開閉／サイドバーhover・active／グローバル検索／CV着信トースト／ActionCable接続状態／セッション切れモーダル。

### 7-3. インベントリの作り方（観測駆動・§5に接続）
各ルートの採取時に、到達可能な状態を §7-1 の軸で全列挙し、`docs/states/<route>.md` に1状態1行で登録する。列: `| 状態ID | 軸 | 説明 | トリガ(どう操作すると出るか) | 採取(スナップショット有無) | 再現([ ]/[x]) | 備考/推測 |`。観測不能状態は §5-7 に従い「手構築/推測」と明記。

### 7-4. 完了の定義（この節が受け入れの母集合）
ルートの完了 = そのルートの `docs/states/<route>.md` の**必須状態**が再現[x]。必須状態: normal / empty / loading / error / 存在する全モーダル・確認ダイアログ / 一覧系は page-last・filter-empty / 権限ゲートがあれば access-denied / pc・sp。任意状態（レアなtooltip等）は努力目標。

---

## 8. 単一トレーサビリティ台帳（完全再現の計測基盤）

状態インベントリ・route→screen突合・エンドポイントカタログ・エンティティ・キャプチャmanifest・スクショベースラインが**バラバラに並立すると相互にドリフトし、「完全の完了」を一意に計測できない**。よって **1状態=1行で全属性を横に持つ正準台帳** `docs/traceability.md` を成果物の中心に置く。

列: `route | slug | state_id | 実画面(screen) | 叩くエンドポイント | 参照エンティティ | fixture | 視覚ベースライン(png) | 必須? | 採取済 | 再現済 | 視覚合格 | インタラクション合格 | 残差(対象外理由) | 備考`

- すべての追跡系（§6/§7/§9/§10/§13）はこの台帳の列として畳み込む。個別mdは詳細を持つが、**進捗の正準はこの台帳**。
- 「完全再現達成」（§1-2）は、この台帳で**対象内状態が全数合格・残差が個別列挙**されていることをもって判定する。
- `docs/traceability.md` のスターター（ヘッダ＋エディタ行の例）を土台（P1）で作る。

---

## 9. 難所（高リスクコンポーネント）と対応方針

到達可能度（正直な評価）: 8コンポーネント中7つは「触って動く完全再現」に十分到達可能。到達不能な残差（主にエディタの1:1 WYSIWYGと集計値の正確性）は §1-2[7]・§13 で明示的に切り出す。

### 9-1. LPエディタ ＝ `/ab_tests/:uid/articles`（最難関・中核）
到達目標を「開けるだけのガワ」から「**本物のLPが表示され主要編集が動く**」へ引き上げる（新事実を活用）。**4段階**:

1. **ガワ完全再現**: レンダ済みDOM＋実CSS（§5）で3レール（左=基本情報/Version/ポップアップ/レポートのタブ、中央=PC 620×486＋SP 430×640 の iframe枠、右=編集ツール群）を再現。
2. **Version/配信割合UI**: 左Versionパネルを再現（各Version名＋配信割合(数値入力)＋更新＋Version追加、ページ状態バッジ）。モックの Version に `distribution_ratio` を持たせ、更新はイミュータブルに新配列を返す。合計が100%でない場合は警告（境界バリデーション）。
3. **プレビューiframeへ実LP注入**: 匿名化済みの実LP HTML（§5-5）を iframe へ `srcdoc` 注入し中央プレビューに表示。PC/SP枠で同一LPをビューポート幅違いで描画。注入前に外部トラッカを除去し外部発信0を保つ。相対URL・画像は合成アセットへ差し替え。
4. **編集操作（ツール別到達水準）**:
   - コード編集: CodeMirror(またはMonaco)で現iframe HTMLを開き、保存で再注入。→**本当に動く**
   - undo/redo: iframe HTMLのスナップショットをイミュータブルなスタックで戻る/進む。→**本当に動く**
   - 画像追加: File→ObjectURL/base64の`<img>`を挿入。→**本当に動く**
   - テキスト追加/編集: 選択要素へcontenteditable付与 or 新規テキストブロック挿入。→**動く（挙動は独自）**
   - ブロック追加: 採取したブロックテンプレHTMLをライブラリ化し挿入。→**動く（品揃えは採取範囲に依存）**
   - 公開: モックへPOST。状態バッジを「準備中→公開中」へ更新（実反映はしない）。→**状態遷移のみ動く**
- **再現不能範囲の明示（§1-2[7]）**: 要素ピッカーの当たり判定・ドラッグハンドル・要素別プロパティパネルの全分岐・ブロックライブラリ内部ロジックは難読化バンドル内で観測不能。**「1操作1操作の同一」は対象外**。到達天井＝「ガワ完全一致＋実LPプレビュー＋動作する独自編集レイヤー」。
- **受け入れ**: 3レールが崩れ無く一致／中央PC・SP枠に実LPがsrcdocで表示され実物と見分けつかない／配信割合変更→更新→反映・Version追加・状態バッジ／コード編集・undo/redo・画像/テキスト追加が動く／外部トラッカがプレビュー経由でも一切ロードされない。

### 9-2. ダッシュボードのチャート（Recharts）
同一OSSを使用。採取: 各チャートの series数・データキー・軸フォーマッタ・tooltip書式・線/面色・凡例位置・grid有無。KPIカード（売上/配信金額/粗利/PV/CLICK/CV/ROAS/ROI）は数値と前日比の書式まで。期間切替（今日/昨日/過去3日/過去7日/今週/今月/カスタム）はLitepickerでUI再現し、選択に応じモック時系列（§10-5）を差し替える。
受け入れ: 折れ線/KPIが視覚一致／期間切替でグラフ差替え／tooltip/凡例/軸書式が一致。

### 9-3. CV速報（ActionCable）― `/conversions`
本番WSには繋がない。**採取は二段構え**: まず `/cable` のWSフレームを観測し channel識別子とCVイベントpayloadを採取。CVは観測中に発火する保証が無いため、採れない場合は `/conversions` REST応答からpayloadを逆算する（両手順を `docs/api` に「採取/逆算」の別で明記）。ローカルWSモック（§10-7）が ActionCable封筒で数秒おきに合成CVをpushして「流れてくる」体験を再現。
受け入れ: CVが数秒おきに上から流れて追加される／メッセージ形状が採取値（不可なら逆算値）と一致／本番WSへ一切接続しない。

### 9-4. ヒートマップ ― `/ab_tests/:uid/.../heatmaps/comparisons`
密度算出アルゴリズムは観測不能かつ実データ非使用のため**再現対象外**。到達＝比較UIのガワ: 2枚のLPプレビュー（§9-1[3]と同じsrcdoc注入）＋canvasの合成density overlay（クリック/スクロール）＋切替トグル・凡例・スケール。
受け入れ: 2LP横並び比較のレイアウト一致／オーバーレイON/OFF・種別切替が動く／数値・密度は全て合成。

### 9-5. スプリットテスト設定6種
共通コンポーネント（条件テーブル＋トグル＋割合入力）を1つ作り使い回す。各ページで採取するのは選択肢データのみ（devices=端末種別 / oses=OS / carriers=キャリア / hours=時間帯粒度 / periods=期間定義UI / params=URLパラメータ条件）。割合は合計100%バリデーション共通実装。
受け入れ: 6ルートが同一コンポーネントで表示され選択肢のみ異なる／トグルと割合入力が動き合計バリデーションが効く。

### 9-6. フォーム（`/folders/forms`, `/folders/:folder_uid/forms/:uid`）
採取時に「組み立て型ビルダーか単純フォームか」を判定し `docs` に記録。フィールド型（テキスト/メール/電話/選択/同意チェック等）とバリデーション文言を採取して再現。組み立て型ならフィールド追加/並べ替えは §9-1 と同方針（動くが内部実装は独自・ドラッグ微挙動の完全一致は対象外）。
受け入れ: フィールドとバリデーション表示が一致／送信はモックで受け（実送信しない）成功/失敗表示。

### 9-7. 画像アップロード / メディア
採取時に「単発input型か、メディアライブラリ型か」を判定。単発: File→ObjectURL/base64で即プレビュー、モックへPOST（実保存しない）。ライブラリ型: ギャラリーUIを再現し合成サムネを並べる。アップロード物はローカル保持のみ。
受け入れ: ファイル選択→プレビュー表示／(ライブラリ型なら)合成サムネのグリッド表示・選択／本番ストレージへ発信0。

### 9-8. 第三者ウィジェット（Channel.io/Pendo等の可視UI）
外部SaaSは**入れない**（§4-4）が、画面に見えているチャットバブル・プロダクトツアー等は「**静的ガワのみ再現 or 省略**」を台帳で線引きする。既定＝チャットバブルは静的ダミーで見た目のみ再現、ツアー等は省略。

---

## 10. データモデル / モックAPI仕様（心臓部・先に固定する）

全82ルート×全状態がインタラクティブに動くかは、事実上すべてモックの網羅性で決まる。**モックを後回しにせず先に固定**する。

### 10-1. モックサーバー方針
- Node製の軽量サーバー（Express or Vite dev middleware）を `mock-server/` に置く。フロントのAPIベースURLは環境変数 `VITE_API_BASE=http://localhost:4010`（例）に固定。本番ドメインは登場させない。
- 3系統（api / api-workers / report）と WS(`/cable`) を**localhostの同一モックにパスプレフィックスで集約**（`/api/*`, `/workers/*`, `/report/*`, `/cable`）。
- レスポンスは匿名化済み `fixtures/`（全て架空）から返す。ページネーション/ソート/フィルタ/検索に実際に応答する（§10-6）。

### 10-2. 主要エンティティとフィールド（FKグラフ・値は全て架空）
親子(FK): Team 1-* Member / Team 1-* Folder / Folder 1-* AbTest / AbTest 1-* Article / Article 1-* Version / AbTest *-1 Media / AbTest 1-* RedirectPage / AbTest 1-* ExitPopup / AbTest 1-* SplitTestSetting / AbTest 1-* Report / AbTest 1-* Conversion / Folder 1-* ConversionTag / Folder 1-* Form / Team 1-* AdAccount / Team 1-* AspAccount / Team 1-* Domain / Team 1-* Tag / Team 1-* Plan / Team 1-* Task / Team 1-* Inspection。

主要フィールド（型）:
- **Team** {id, uid, name, plan_id}
- **Member** {id, uid, name, email, role, team_id}
- **Folder** {id, team_id, uid, name, parent_id|null, ab_tests_count}
- **AbTest** {id, team_id, uid, title, memo, media_id, folder_id, published:bool, ad_status, editor_version, created_at, updated_at, media{attributes{id,name,icon_name,ad_cooperation}}, folder{...}, creator{id,uid,name}}
- **Article** {id, uid, ab_test_id, memo, archived:bool, style_applied:bool, created_at, updated_timestamp}
- **Version** {id, uid, article_id, name, distribution_ratio(0-100), status('準備中'|'公開中'|'停止'), is_control:bool, html, css, thumbnail_url, updated_at} ※html/cssは捕捉した実LPを架空文言に置換して格納
- **Media** {id, name, icon_name, ad_cooperation:bool}
- **RedirectPage** {id, uid, ab_test_id, name, url, weight, enabled:bool}
- **ExitPopup** {id, uid, ab_test_id, name, trigger, html, enabled:bool}
- **SplitTestSetting** {id, ab_test_id, type('devices'|'oses'|'carriers'|'hours'|'periods'|'params'), rules:[{key,label,ratio,enabled:bool}]}
- **Report(集約行)** {scope('ab_test'|'version'|'lp'|'creative'), entity_uid, date, sales, ad_cost, gross_profit, pv, click, cv, roas, roi, cvr, cpa} ※派生は §10-5 の恒等式
- **Conversion** {id, uid, ab_test_uid, version_uid, media_id, amount, occurred_at, status}
- **ConversionTag** {id, uid, folder_id, name, tag_type, snippet}
- **Form** {id, uid, folder_id, name, fields:[{name,type,required:bool}]}
- **Task** {id, uid, team_id, title, assignee_member_id, status, due_at}
- **Inspection** {id, uid, team_id, authority, folder_id, status, submitted_at}
- **AdAccount** {id, uid, team_id, provider('facebook'|'google'|'microsoft'|'x'|'yahoo'), account_name, connected:bool, connected_at}
- **AspAccount** {id, uid, team_id, asp_name, connected:bool}
- **Domain** {id, uid, team_id, host, status('active'|'pending'|'error'), ssl:bool}
- **Tag** {id, uid, team_id, name, color}
- **Plan** {id, uid, team_id, name, price, seats, current:bool} / **Addon** {id, uid, name, price, enabled:bool}
- **SbAiConversation** {id, uid, title, created_at} / **SbAiMessage** {id, conversation_id, role('user'|'assistant'), content, created_at}
- **Heatmap** {id, ab_test_uid, version_uid, type('click'|'scroll'), points:[{x,y,value}], thumbnail_url}
- **MediaAsset(画像ライブラリ)** {id, uid, team_id, url, width, height, name}

### 10-3. エンドポイント網羅カタログ（全82ルート連動・採取漏れ0が受け入れ条件）
系統: [A]=api, [W]=workers, [R]=report, [WS]=/cable。全て localhost 集約。

| ルート | メソッド エンドポイント | 系統 | レスポンス形状 |
|---|---|---|---|
| `/`,`/dashboard`,`/dashboard/:uid` | GET /teams/dashboard?start_date&end_date | A | {kpi:{sales,ad_cost,gross_profit,pv,click,cv,roas,roi}, series:[{date,...KPI}], new_ab_tests:[], new_versions:[]} |
| dashboard内ランキング | GET /ab_tests/rankings?start_date&end_date&sort&sort_direction&per_page | A | {pagination,ab_tests:[{...,sales,roas}]} |
| dashboard内Versionランキング | GET /teams/version-rankings?start_date&end_date&sort_by&sort_order&per_page | W | {pagination,versions:[{...,sales}]} |
| `/folders` | GET /folders?per_page | A | {pagination,folders:[{...,ab_tests_count}]}（木はparent_id） |
| `/folders/:uid/edit` | GET/PUT /folders/:uid | A | {folder:{...}} |
| `/folders/forms`,`/folders/:uid/forms/:uid` | GET /folders/:uid/forms, GET /folders/:uid/forms/:uid | A | {forms:[]}/{form:{...}} |
| `/folders/:uid/conversion_tags` | GET /folders/:uid/conversion_tags | A | {conversion_tags:[]} |
| `/folders/:uid/operator_articles(/:uid/edit)` | GET /folders/:uid/operator_articles(/:uid) | A | {operator_articles:[]}/{...} |
| `/ab_tests/:uid/articles`(=LPエディタ) | GET /ab_tests/:uid, GET /ab_tests/:uid/articles, GET /articles/:uid/versions | A | {ab_test},{articles:[]},{versions:[{...,distribution_ratio,status,html,css}]} |
| (エディタ保存/割合/追加) | PUT /versions/:uid, PATCH /versions/:uid/distribution, POST /articles/:uid/versions, POST /versions/:uid/publish | A | {version:{...}} |
| (エディタ画像) | GET /teams/media_assets | A | {media_assets:[]} |
| `/ab_tests/:uid/articles/:uid/previews` | GET /articles/:uid/previews | A | {preview:{html,css}} |
| `/ab_tests/:uid/articles/exit_popups` | GET/POST/PUT /ab_tests/:uid/exit_popups | A | {exit_popups:[]} |
| `/ab_tests/:uid/redirect_pages`, `/folders/.../redirect_pages` | GET/PUT /ab_tests/:uid/redirect_pages | A | {redirect_pages:[]} |
| `/ab_tests/:uid/articles/split_test_settings/{devices,oses,carriers,hours,periods,params}` | GET/PUT /ab_tests/:uid/split_test_settings/:type | A | {split_test_setting:{type,rules:[]}} |
| `/ab_tests/:uid/options/devide` | GET/PUT /ab_tests/:uid/options/devide | A | {devide:{...}} |
| `/ab_tests/:uid/reports` | GET /ab_tests/:uid/reports?start_date&end_date | A | {rows:[Report(scope=version)], totals:{...KPI}} |
| `/ab_tests/:uid/reports/lp` | GET /ab_tests/:uid/reports/lp | A | {rows:[Report(scope=lp)]} |
| `/ab_tests/:uid/reports/swipe`, `/ab_tests/:uid/versions/swipe` | GET /ab_tests/:uid/reports/swipe | A | {rows:[]} |
| `/ab_tests/:uid/creative_report` | GET /ab_tests/:uid/creative_report | A | {rows:[Report(scope=creative)]} |
| `/ab_tests/:uid/articles/htmls/heatmaps/comparisons` | GET /ab_tests/:uid/heatmaps/comparisons | A/W | {heatmaps:[{type,points,thumbnail_url}]} |
| `/conversions` | GET /conversions?per_page + [WS] ConversionsChannel | A/WS | {conversions:[]} + WS push |
| `/conversion-reports` | GET /conversion-reports?start_date&end_date | A | {rows:[]} |
| `/tasks` | GET/POST/PUT /tasks | A | {tasks:[]} |
| `/inspections`,`/inspections/authorities`,`/inspections/folders` | GET /inspections, /inspections/authorities, /inspections/folders | A | {inspections:[]} 等 |
| `/sb_ai`,`/sb_ai/chat`,`/sb_ai/chat/:id` | GET /sb_ai/conversations, GET /sb_ai/conversations/:id, POST /sb_ai/conversations/:id/messages(SSE) | A | {conversations:[]}/{messages:[]}/stream |
| `/teams/ad_accounts` | GET /teams/ad_accounts | A | {ad_accounts:[]} |
| `/teams/asp_accounts` | GET /teams/asp_accounts | A | {asp_accounts:[]} |
| `/teams/domains` | GET /teams/domains | A | {domains:[]} |
| `/teams/tags` | GET /teams/tags | A | {tags:[]} |
| `/teams/product_search_forms` | GET /teams/product_search_forms | A | {product_search_forms:[]} |
| `/teams/plans` | GET /teams/plans | A | {plans:[], current_plan_id} |
| `/teams/:uid/members/:uid/invitations/edit` | GET/PUT /teams/:uid/members/:uid/invitation | A | {invitation:{...}} |
| `/redirections/ad_accounts/{facebook,google,microsoft,x,yahoo}` | GET /redirections/ad_accounts/:provider/status | A | {provider, connected:bool, message}（OAuth結果画面用） |
| `/redirections/ab_tests/google_postback_setting` | GET /redirections/ab_tests/google_postback_setting | A | {status} |
| `/settings/internal_notifications/{member,team}` | GET/PUT /settings/internal_notifications/:scope | A | {settings:{...}} |
| `/report-exclusions` | GET/POST/DELETE /report-exclusions | A | {report_exclusions:[]} |
| `/plans/:uid/payment/checkout` | GET /plans/:uid, POST /plans/:uid/checkout | A | {plan},{ok:true} |
| `/addon/option-list` | GET /addon/option-list | A | {addons:[]} |
| `/users/edit` | GET/PUT /users/me | A | {user:{...}} |
| `/users/public_api_key` | GET/POST /users/public_api_key | A | {public_api_key} |
| `/users/teams` | GET /users/teams | A | {teams:[]}（テナント切替） |
| `/users/sign_up`,`/confirmation`,`/confirmation/code/new`,`/forgot_password`,`/password/edit` | POST 各Devise規約ルート | A | {ok:true}（モック認証・§9系ではなく認証擬似） |
| `/admin-report` | GET /admin/report | A | {rows:[]} |
| `/admin/articles/html_parts` | GET /admin/articles/html_parts | A | {html_parts:[]} |
| `/admin/plans(/new)` | GET/POST /admin/plans | A | {plans:[]} |
| `/admin/preset_access_denials` | GET /admin/preset_access_denials | A | {items:[]} |
| `/admin/product_search_forms` | GET /admin/product_search_forms | A | {items:[]} |
| `/admin/teams(/new,/:uid/members,/:uid/plans/payments)` | GET/POST /admin/teams 系 | A | {teams:[]} 等 |
| `/permissions` | GET /permissions | A | {permissions:[]} |
| `/introductions` | GET /introductions | A | {introductions:[]} |
| `/seminar` | GET /seminar | A | {seminars:[]} |
| `/terms` | GET /terms（静的） | A | {html} |
| ランキング(外部) | GET /rankings 相当 | R | {rows:[]}（別ドメインをローカルにミラー） |

※実装は付録Aの82ルートにこの表を突き合わせ、**全ルートがモックで200応答すること**をテストで機械保証する（§13-B）。

### 10-4. 認証フロー（モック認証）
Devise系（confirmation/code/new/forgot_password/password/edit）は**モック認証**。任意入力でログイン成功扱い→ダッシュボードへ。実トークン発行・メール送信はしない（§3-2隔離）。多段（サインアップ→confirmation→code/new）は各ステップ＋各バリデーションエラーを状態として台帳に展開。

### 10-5. 合成データ生成戦略（決定論・KPI恒等式）
- **決定論seed**: faker等を固定seed（例 SEED=42）で初期化。再起動しても同一データを再生（FK整合）。id/uidもseedから決定生成。
- **既定シード＝新規アカウント（空）**（§1-4・§16-4で確定）: 起動時は「発行直後の新規アカウント」を再現する空状態。User 1 / Team 1 / Member 1（本人） / Folder 0（or 既定フォルダ1のみ） / AbTest 0 / Article 0 / Version 0 / Conversion 0 / Task 0 / AdAccount 0 / Domain 0 / Plan は初期プラン1。**Media（媒体）ロスターだけは選択肢として固定保持**（作成時に選べるように）。以降はユーザー操作で作られたものがセッション内に増える（§10-9）。
- **（任意）demo/実データ取込シード**: 見た目確認や後日の実データ取込用に、populated なシード（AbTest 数十件等）を別モードとして用意してよいが、**既定ではない**。KPI恒等式・決定論seedは両モード共通。
- **媒体ロスター（固定）**: Facebook/Instagram/LINE/TikTok/Google/Yahoo/X/その他。icon_name は FA/Material の該当名。
- **時系列（チャート/レポートが常に非空）**: 各AbTest×日付で日次メトリクスを生成。期間切替のどれを選んでも該当範囲を集計。基準日は起動日（相対生成）。
- **KPI恒等式（内部矛盾を出さない）**: 各行で ad_cost・cv・click・pv を一次生成し、派生を必ず次式で算出 — `gross_profit = sales - ad_cost` / `roas = sales / ad_cost` / `roi = gross_profit / ad_cost` / `cvr = cv / click` / `cpa = ad_cost / cv`。`sales = cv × 平均単価`。ゼロ除算は「-」表示。合計行は各列合算（roas/roi/cvr/cpaは合算後に再計算）。
> これは §1-1 の「バックエンド集計は再現しない」と両立する。**サーバー集計ロジックは再現しないが、表示が矛盾しないための決定論的擬似生成規則は持つ**、という位置づけ。

### 10-6. 一覧応答契約（「触れるが効かない」を禁止）
- 共通クエリ: `per_page`(既定20), `page`/`current_page`, `locale=ja`, `referer=`（後2つは無視可）を受理。
- pagination: `{total_pages, total_count, current_page}` を実件数から算出して返す。
- ソート（エンドポイント別に規約が違う点に注意）: `/ab_tests/rankings` は `sort`&`sort_direction`、`/teams/version-rankings`(workers)は `sort_by`&`sort_order`。各エンドポイントは自分の規約を honor。汎用一覧は `sort`&`sort_direction`。
- フィルタ: `media_id`/`folder_id`/`published`/`ad_status`/`status` が来たら実際に絞り込む。
- 検索: `q`/`keyword` で title/name/memo を部分一致。
- 日付範囲: `start_date`/`end_date` で時系列/レポートを範囲集計。未指定は既定期間（過去7日）。
- **並べ替え・分割・絞り込みは必ずモック側で行い**、フロントはそのまま描画（実サイトと同じ挙動）。

### 10-7. ActionCable(`/cable`)モックのメッセージ形式
ローカルWSモックは Rails ActionCable の封筒を忠実に模倣。
- 接続直後(→client): `{"type":"welcome"}`
- 定期ping(数秒毎): `{"type":"ping","message":<unix>}`
- 購読(client→): `{"command":"subscribe","identifier":"{\"channel\":\"ConversionsChannel\"}"}`
- 購読確認(→): `{"type":"confirm_subscription","identifier":"{\"channel\":\"ConversionsChannel\"}"}`
- CV速報push(→、3-8秒ランダム): `{"identifier":"{\"channel\":\"ConversionsChannel\"}","message":{"conversion":{"uid","ab_test_uid","ab_test_title","version_name","media":{"name","icon_name"},"amount","occurred_at"}}}`
- `/conversions` は初期 GET で既存分を描画し、WS pushで先頭に追加。
- sb_aiチャットのストリーミングは WS ではなく **SSE**（POST /sb_ai/conversations/:id/messages が text/event-stream でトークン逐次返し）でモック。

### 10-8. 全状態を「触って」出すためのモック制御
`?mock_state=empty|loading|error|success` を任意エンドポイントに付けると、空配列 / 意図的遅延(例2s) / 4xx-5xxエラー封筒 / 正常 を返す。エラー封筒は `{"error":{"code","message"}}`（実サイト規約は採取でき次第 `docs/api` に追記）。既定（付与なし）は success。フロントにデバッグ用の状態切替UIは出さない（URL直叩き or 開発用パネルのみ）。§13-C の全状態検証を機械化するための機構。

### 10-9. 書き込みとセッション内永続化（「実際に使える」ため・§1-4）
新規アカウントから実際に使えるよう、モックは GET だけでなく**書き込み（POST/PUT/PATCH/DELETE）を受理し、セッション内で状態を永続**する。
- **保持先**: モックサーバーのメモリ内ストア（既定シード＝空で初期化）。単一ユーザー前提でよい。リロード耐性が要るなら localStorage 併用。
- **反映（FK整合を保つ）**: フォルダ作成／beyondページ作成／Version追加・配信割合変更／LP編集(html・css)／公開(status遷移)／タスク作成 等が「作成 → 一覧に出る → 詳細/エディタで開ける」まで一貫して繋がる。
- **リセット**: 「新規アカウント発行直後（空）」へ戻す reset を用意（再起動 or `?reset=1`）。
- **集計の扱い**: 売上/ROAS等はサーバー集計を再現せず §10-5 のKPI恒等式で擬似生成（§1-1）。作成直後は0件・0値から始まり、ダミーの流入が乗ると数値が付く、という見え方でよい。

---

## 11. 推奨アーキテクチャ（capture-and-rehydrate ハイブリッド・確定）

最大フィデリティ（完全再現）を最優先する前提で、以下の1案に確定する。理由: 実CSS約386KB・実DOM・実LP iframe HTML・各状態スナップショットが同一オリジン・本人ログイン済みChrome経由で機械採取できる（§4-5）。手書き再現は採取できる本物のCSS/DOMを捨てて人手で近似することになり、忠実度が構造的に劣る。

**方式: 採取した実HTML/実CSSを土台(base)にし、モックへ再配線したうえで、インタラクションと可変状態を持つ「島(island)」だけ動的に再実装する。**
- **土台(静的)**: 各ルートで `#root` のレンダ済み `outerHTML` を採取→匿名化→ルートごとの実体HTMLとして配置。CSSは実ファイル（normalize / index-main 322KB / index-sub 51KB / icons 他）を verbatim（匿名化のみ）でローカル配信。Emotionのハッシュ付きクラスは単一ビルド内で安定しているので**真似ず・捨てず「そのまま使う」**。→ 視覚忠実度は事実上ピクセル一致。
- **サニタイズ層(自動・必須)**: 5.8MBバンドルと外部SaaSタグを除去。実データを合成へ全置換。全 `*.squadbeyond.com` URL と `/cable` を localhost へ（§5-5）。
- **インタラクション層(手書き・薄い)**: タブ/ソート/ページネーション/モーダル/フォームバリデーションを、採取DOMのクラスを狙って再注入。データ駆動一覧はモックから再レンダ、構造的状態（モーダル開/閉・空/エラー）はスナップショット・トグルで切替。
- **動的な島(フリーズDOMで代替不可・個別再実装)**: LPエディタ（§9-1）、Rechartsチャート（モック時系列で再初期化）、Litepicker、CV速報（ローカルWS）。

**「HTMLベース/CLI構築」要件との整合**: 採取DOMは静的HTMLそのものなので、React全面再構築より厳密に「開いて中身が読めるHTML」要件を満たす。採取はClaude in Chrome、匿名化/組み立て/モック配線/Vite多ページ配信はClaude Code(CLI)で完結。

| 観点 | (a) capture-rehydrate ハイブリッド【採用】 | (b) 素のマルチページ手書き | (c) React+Vite全面 |
|---|---|---|---|
| 視覚忠実度 | 最高（本物CSS/DOMをそのまま使用＝ほぼピクセル一致） | 低（322KB CSSの人手近似でドリフト不可避） | 中〜低（結局CSS手書き） |
| 完全性（全ルート/全状態） | 最高（機械採取で全ルート・全状態を面で取得） | 実装分だけ | 実装分だけ |
| 工数 | 中（採取は安い/島の再実装が主コスト） | 最大（82枚手作り） | 最大 |
| HTMLベース要件 | ◎（採取DOM＝実HTML） | ◎ | △（SPA化に注意） |

**採取ビルドの固定（必須）**: 本方式は単一ビルドに依存する。採取時のメインJS/CSSハッシュ（例 `index-cb391eb6.css`）を `docs/capture-manifest.md` に記録し、全採取物のビルドを一致させる。オリジナルが再ビルドされたら該当ルートのみ再キャプチャする運用とする。

### ディレクトリ構成 → §5-3 参照（多数の小さいファイル・1ファイル200-400行、最大800行）。

---

## 12. コーディング規約（全社規約の転記・遵守必須）

- **推測で埋めない**: 曖昧なら止まって §16 を見て人間に確認（2〜4択＋推奨1つ）。存在しないAPI/関数を"ある前提"で書かない。決定を勝手に蒸し返さない。
- **機械チェックで証明してから「できた」と言う**: 型（tsc）・lint（eslint）・テスト（vitest等）を実際に走らせ緑を確認してから完了報告。目視で「直った」と言わない。テスト基盤が無ければ手動確認手順を正直に書く。
- **TDD**: バグ修正時は先に失敗するテストを書き、緑化を直った証拠にする。
- **症状でなく根本原因**: try/catch握りつぶし・表示側だけ繕う・不要な `if(x==null)return` を"念のため"で足さない。
- **KISS / DRY / YAGNI**: 最もシンプルで実際に動く解を。重複が現実にあるときだけ抽出。使う前に抽象化しない。
- **イミュータブル（重要）**: 既存オブジェクトを破壊的変更しない。常に新しいコピーを返す。
- **多数の小さいファイル**: 200-400行標準、最大800行。機能/ドメインで整理。高凝集・低結合。
- **エラーハンドリング**: 全レベルで明示的に。UI面は優しいメッセージ、内部は詳細ログ。握りつぶし禁止。
- **入力バリデーション**: 境界（API応答・入力・ファイル）で処理前に検証。外部データを信用しない。
- **命名**: 変数/関数 camelCase、真偽 is/has/should/can、型/コンポーネント PascalCase、定数 UPPER_SNAKE_CASE、フック use 接頭。
- **セキュリティ**: シークレットをソースにハードコードしない。`.env`／認証情報／生キャプチャは絶対にコミットしない。XSS等の基本対策。
- **GitHub**: リポジトリは private 既定。
- **忠実再現 > 社内UI美観規約**: §3-5 参照（勝手にUIを"改善"しない）。

---

## 13. 受け入れ条件（機械判定可能なチェックリスト）

「完全再現達成」はレビュー担当の主観でなく、以下を全て自動で満たすことで判定する。`npm run verify`（Playwright＋静的スキャン）一撃で回るようにする。

### 13-A. 視覚一致率（全ルート×必須状態）
- [ ] 各（ルート×必須状態）で、匿名化済みキャプチャを基準画像とし、クローンのフルページスクショを Playwright＋pixelmatch で比較。ビューポートは 1440×900(PC) と 430×932(SP)。
- [ ] 動的領域（Rechartsチャート/日時/CVカウンタ/ランダムfixture/エディタiframe内）はマスクして除外。
- [ ] 非マスク領域の不一致ピクセル ≤ 1.0%（SSIM ≥ 0.98）。（capture方式＋実CSS使用のため厳格閾値を要求できる）

### 13-B. 全ルート到達（82/82）
- [ ] 付録Aの全82ルートを巡回するクローラが、各ルートで `#root` 非空・未捕捉console error 0・404フォールバックは `*` のみ、を確認。パラメータ付きは固定ダミーuidで解決。
- [ ] §10-3 のエンドポイントカタログと突合し、全ルートがモックで200（または意図した4xx/5xx）を返し、spinner固定・未定義404が1件も無い。

### 13-C. 全状態表示（状態インベントリ準拠）
- [ ] `docs/states/<route>.md` に各ルートの必須状態が列挙され、各状態がクローン上で実際にトグル・レンダされることを自動テストで確認（採取した各状態スナップショットが基準）。`?mock_state=` で空/読込/エラーを再現。

### 13-D. 全インタラクション & ナビゲーショングラフ
- [ ] `docs/interactions.md` に各ルートの操作（タブ/ソート/ページネーション/モーダル/フォームバリデーション/期間選択/Version配信割合入力 等）を列挙し、各操作を Playwright で実行し期待DOM変化を確認。
- [ ] 一覧のページネーション/ソート/フィルタ/検索がモック側で実際に効く（件数・並び順・絞り込みが変わる）。
- [ ] **ナビグラフ全走行**: 全リンク/ボタンの遷移先をクローラで確認し、404や空ガワの行き止まりが0件。

### 13-E. 実データ非混入（0件）
- [ ] ビルド成果物および全fixtureに対し、denylist（実在従業員氏名・実ドメイン・実施策名・`*.squadbeyond.com`・実金額パターン）を静的スキャンして 0 ヒット。
- [ ] 生キャプチャ隔離ディレクトリの内容物がリポジトリに1件も含まれない（gitトラッキング外）。

### 13-F. 本番ドメイン非接続（0件）
- [ ] 全82ルート巡回中の全ネットワークリクエストを Playwright で記録し、`localhost` 以外のホストへのリクエスト 0 件。
- [ ] ソース/成果物を grep し、`api.squadbeyond.com` / `api-workers.squadbeyond.com` / `report.squadbeyond.com` / 本番 `/cable` 文字列 0 ヒット。

### 13-G. 外部SaaSタグ非ロード（0件）
- [ ] 成果物HTML/JSを grep し Sentry / Pendo / HubSpot / GTM / GA4 / Mixpanel / Channel.io の識別子 0 ヒット。実行時ロード 0 件。

### 13-H. 品質ゲート
- [ ] `tsc` / `eslint` が緑。共通コンポーネントとモッククライアントに単体テストがある。
- [ ] `npm run dev` と `npm run mock` の2コマンドで追加設定なしに起動。
- [ ] リポジトリは private。`.env`・認証情報・生キャプチャがコミットされていない。
- [ ] `docs/capture-manifest.md` に採取ビルドのハッシュが記録され、全採取物のビルドが一致。
- [ ] `docs/traceability.md` で対象内状態が全数合格・残差（§1-2[7]）が個別列挙・承認されている。

> **到達不能領域の明示**: LPエディタの1:1 WYSIWYG実編集・サーバ集計値の正確性・ヒートマップ密度アルゴリズム・観測不能状態（§5-7）は、ブラックボックス再現の限界により擬似/近似で埋める。該当は `docs/pages/*.md` に「推測/擬似/近似」と明記し、上記チェックは「ガワと採取済み状態の再現」までを判定範囲とする（ビット一致は要求しない）。

---

## 14. フェーズ / マイルストーン（状態爆発を織り込んだ再ベースライン）

状態インベントリ導入で母集合が数倍化（300-600状態）するため、**全状態一括必達のオール・オア・ナッシングにしない**。優先度ティアで段階的に「完全」を確定する。

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| **P0. 準備** | リポジトリ初期化、Vite/TS/lint/test/verify 雛形、`docs/`（routes/traceability/states/_taxonomy/capture-manifest）配置、`tokens.css` | `npm run dev` で空ページ、`npm run verify` の枠が動く |
| **P1. 土台** | 共通レイアウト、モックサーバー雛形＋エンティティ/合成データ（§10-2,5）、APIクライアント（localhost固定）、**capture-plan・匿名化パイプライン・トレーサビリティ台帳**を先に用意 | 空シェルが全ルートで表示・404が出ない／台帳の骨格が回る |
| **P2. Tier1 × 主要状態** | ダッシュボード（Recharts＋KPI＋一覧）、`/folders`、認証系。各ルートの必須状態（normal/empty/loading/error/主要モーダル/pc・sp）まで | Tier1が必須状態込みで触れる |
| **P3. Tier2 × 主要状態** | **LPエディタ（§9-1の4段階：ガワ＋Versionパネル＋実LPのsrcdoc注入プレビュー＋コード編集/undo-redo/画像追加/公開）**、記事/レポート/スプリット/離脱ポップアップ/リダイレクト/CV速報WS/ヒートマップ簡易。プレビュー注入は共通ユーティリティとしてP3先頭で実装 | Tier2が必須状態込みで触れる |
| **P4. Tier3 × 主要状態** | チーム/連携/審査/AI/課金/設定/一括系 | Tier3が必須状態込みで触れる |
| **P5. Tier4＋仕上げ** | 管理者・周辺、全ルート×全必須状態の照合、`npm run verify` 緑化、§13全チェック、台帳で残差承認 | §13 全チェック・§1-2 の完全再現達成 |
| **（将来）P6** | LPエディタのWYSIWYG**近似度の向上**（ドラッグ編集・要素ピッカー・コード編集の忠実化。※1:1同一は非目標） | 別企画で見積り |

---

## 15. リスク一覧

| リスク | 影響 | 対策 |
|---|---|---|
| 実データ混入 | 機密漏洩 | §3-1・§5-5。合成データ徹底、grepゲート、生キャプチャ隔離＆非コミット |
| 本番API誤接続 | データ改変・規約違反 | §3-2。localhost固定、本番ドメインgrep 0件保証（§13-F） |
| エディタの底なし工数 | 納期崩壊 | §9-1。到達天井を明記し1:1 WYSIWYGは対象外。P6送り |
| 状態爆発（300-600） | 未完・品質バラつき | §7・§14。ルート×状態＋優先度ティアで段階確定。台帳で計測 |
| 観測できない状態（権限分岐・集計） | "それらしいが不正確" | §5-7・§1-2[7]。残差として列挙、擬似/手構築で埋め「推測」明記 |
| 採取ビルドの陳腐化 | スナップショット齟齬 | §11。採取ビルドhash固定、再ビルド時は該当ルートのみ再撮 |
| 権利/セッション | ToS・資格情報 | §3-4。非公開・非再配布・認可アカウントのみ・資格情報非保存 |

---

## 16. 未確定事項（着手前に人間へ確認・推奨を添える）

1. **スタック最終確定**: §11 の **capture-and-rehydrate ハイブリッド**で確定してよいか（推奨。B=素のマルチページ手書き / C=全面React+Vite は忠実度・工数で劣るため非推奨）。
2. **エディタの今回到達点**: §9-1 の「ガワ＋実LPのsrcdocプレビュー＋コード編集/undo-redo/画像追加/公開が動く」まで（推奨）。1:1 WYSIWYGはP6送りで良いか。
3. **エディタの2ルート関係**: `/ab_tests/:uid/articles` と `/folders/:.../edit` の役割（別入口/別モード/旧版）を観測で確定（実装は同一コンポーネント共有の前提）。
4. **モックデータ規模** ―― 【確定済】既定シードは**新規アカウント（空）＝発行直後**を再現（§1-4・§10-5）。実データ(populated)は後日の取込フェーズ。Media（媒体）ロスターのみ選択肢として固定保持。demo用のpopulatedシードは任意で別モード。
5. **外部ランキング（`report.squadbeyond.com`）**: ローカルにミラー簡易再現でよいか、そこも全状態再現対象に含めるか。
6. **成果物の置き場所/公開**: private GitHub リポジトリ化するか、ローカルのみか。リポジトリ名。
7. **採取の分担**: 各ルート×状態のデベロッパーツール採取を、人間・Claude in Chrome拡張・実装AIのどれが担うか（§5-2。拡張はログイン済みChromeが必要）。
8. **出力面（CSV/PDF/印刷/メール）**: これらのガワ/挙動を再現対象に含めるか、省略か（§7-1）。
9. **観測不能状態の代替**: 権限差分等（§5-7）を第2アカウントで採取するか、CSS＋スキーマから手構築するか。

---

## 付録A. 全82ルート一覧（機械可読・進捗は §8 台帳で管理）

```
# 認証 / ユーザー
/users/sign_up
/users/confirmation
/users/confirmation/code/new
/users/forgot_password
/users/password/edit
/users/edit
/users/public_api_key
/users/teams

# ダッシュボード
/
/dashboard
/dashboard/:uid

# beyondページ（ab_test）  ※/ab_tests/:uid/articles は LPエディタ本体（§6-2）
/ab_tests/:ab_test_uid/articles
/ab_tests/:ab_test_uid/articles/:article_uid/previews
/ab_tests/:ab_test_uid/articles/exit_popups
/ab_tests/:ab_test_uid/articles/htmls/heatmaps/comparisons
/ab_tests/:ab_test_uid/articles/split_test_settings/carriers
/ab_tests/:ab_test_uid/articles/split_test_settings/devices
/ab_tests/:ab_test_uid/articles/split_test_settings/hours
/ab_tests/:ab_test_uid/articles/split_test_settings/oses
/ab_tests/:ab_test_uid/articles/split_test_settings/params
/ab_tests/:ab_test_uid/articles/split_test_settings/periods
/ab_tests/:ab_test_uid/creative_report
/ab_tests/:ab_test_uid/options/devide
/ab_tests/:ab_test_uid/redirect_pages
/ab_tests/:ab_test_uid/reports
/ab_tests/:ab_test_uid/reports/lp
/ab_tests/:ab_test_uid/reports/swipe
/ab_tests/:ab_test_uid/versions/swipe

# フォルダ
/folders
/folders/forms
/folders/:uid/edit
/folders/:folder_uid/ab_tests/:ab_test_uid/edit
/folders/:folder_uid/ab_tests/:ab_test_uid/redirect_pages
/folders/:folder_uid/conversion_tags
/folders/:folder_uid/forms/:uid
/folders/:folder_uid/operator_articles
/folders/:folder_uid/operator_articles/:uid/edit

# 記事一括 / コンバージョン
/articles/bulk_replaces
/conversions
/conversion-reports

# タスク / 審査
/tasks
/inspections
/inspections/authorities
/inspections/folders

# AI
/sb_ai
/sb_ai/chat
/sb_ai/chat/:conversationId

# チーム / 連携
/teams/ad_accounts
/teams/asp_accounts
/teams/domains
/teams/plans
/teams/product_search_forms
/teams/tags
/teams/:team_uid/members/:member_uid/invitations/edit

# 広告OAuthコールバック
/redirections/ab_tests/google_postback_setting
/redirections/ad_accounts/facebook
/redirections/ad_accounts/google
/redirections/ad_accounts/microsoft
/redirections/ad_accounts/x
/redirections/ad_accounts/yahoo

# 設定 / 通知
/settings/internal_notifications/member
/settings/internal_notifications/team
/report-exclusions

# 課金 / アドオン
/plans/:planUid/payment/checkout
/addon/option-list

# 管理者
/admin-report
/admin/articles/html_parts
/admin/plans
/admin/plans/new
/admin/preset_access_denials
/admin/product_search_forms
/admin/teams
/admin/teams/new
/admin/teams/:team_uid/members
/admin/teams/:team_uid/plans/payments

# その他
/permissions
/introductions
/seminar
/terms
/cable        # WebSocket（ActionCable。UIルートではない・WSモックで再現）
*             # 404 キャッチオール
```

---

## 付録B. 調査の再現方法（採取係が同じ観測をやり直す手順）

1. Claude in Chrome拡張が入った、本番にログイン済みのChromeで `https://app.squadbeyond.com/` を開く。
2. メインJSバンドル（`/assets/index-<hash>.js`）を `fetch` し `path:"..."` を正規表現抽出 → 全ルート（本書は82件取得）。ナビ全巡回で「未知ルート0」を証明（§6-1）。
3. **アプリ全CSSの一括取得**: `normalize.css` / `/assets/index-cb391eb6.css`(322KB) / `/assets/index-05deeb9d.css`(51KB) / アイコンCSS 他、計約386KB を丸ごと fetch（全HTTP200）。
4. 各ルート×各状態で、レンダ済み `outerHTML`・LPプレビューiframe文書（同一オリジンでDOM可）・Networkの各APIレスポンス・フルページスクショを採取（§5-1）。
5. 採取物を隔離ディレクトリへ保存→匿名化（§5-5）→ `capture/clean/` へ昇格→ §5-6 で再現。

（本文書の技術情報はすべて 2026-08-31 時点の実測。オリジナルの更新で変わりうる。採取ビルドの hash を `docs/capture-manifest.md` に固定すること。）
