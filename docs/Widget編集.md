# Widget編集（1つの画面）の決まり

2026-09-23 に「ノーコードで作る」（ライブラリの中の入力画面）と「Widget編集」（全画面）を1つにした（本人の決定 D1〜D3）。
この文書は、その画面の作り・保存の形・触るときの決まりの正本。詳しい理由は各ファイル先頭のコメントに書いてある。

## 画面の作り（widget-studio.ts）

全画面。**左＝見え方（620pxの見たまま画面）／右＝直すところ**。この左右は崩さない（本人指定）。

右は3段:

1. **画面のタブ**（`[data-widget-tabs]`）… 画面①②…
2. **部品** … 画面の設定／部品の一覧（選んだ1つを広げる）／部品を足す／Widget全体の設定（template-form.ts）
3. **デフォルト時のコードを表示** … Widget全体のHTML/CSS（widget-code-panel.ts。設定から書き出したものなので見るだけ）

ヘッダー: 閉じる／作成したWidgetに登録／**LPに入れる**（新しく作るとき）または **更新する**（LPの中のWidget）。

## 直し方は1つ（部品）— 2026-09-24 第3弾

本人の指摘「Widget編集とノーコードで作るで画面UIが2つある。ノーコードの方に合わせて、書式のツールバーだけは残して」。
以前あった「HTMLモード（要素ごとのカードとコードを右に出す）」は無くし、**どのWidgetも右は「部品」**。

| 開くもの | どう開くか |
|---|---|
| 部品で作ったWidget（外側に `data-nc-data`） | その設定データで開く |
| 設定データを持たないWidget（自作の見本・手書きのHTML・2026-09-23 より前の部品Widget） | `wrapHtmlAsBuilder`＝**見本の部品1つ**にして開く（余白0・背景「なし」で見え方は変えない）。部品が1つだけなら開いた直後にその部品を広げる |

見本の部品の中（form-sample.ts）に、以前のWidget編集にあった直し方を全部置いた:
- 文字・画像・動画・ボタン・囲み の欄と「押したとき」、設問①②…のタブ（前からあるもの）
- **見た目の細かい設定** ＝ 要素ごとのカード（widget-design-panel.ts。色・大きさ・余白・動き。数字はドラッグ＋スライダー）
- **コードで直す** ＝ この見本のHTML・CSS（色付きのコード欄。打ち終わると入力欄を組み立て直す）

左の見たまま画面:
- **書式のツールバーは常に出す**（太字・色・大きさ・フォント・リンク・画像…）。見出し・文章・ボタンなどの部品の文字も、ツールバーで付けた飾りごと持つ（`rich-text.ts`。持てるのは文字の飾りだけ＝b/i/u/s/span/font/br と色・大きさの style。script・img・a などは外す）
- 見本の部品の中を押すと、右でその部品が開き「見た目の細かい設定」がその要素のカードだけになる。左の枠もその要素に付く
- 画像の操作パネル・リンクの吹き出し・並んだ部品の複製/上下/消す は**見本の部品の中だけ**効く（見出しやボタンの部品は右の欄で直す）
- 道具が中身を変えたら `notifyCanvasEdit(contentDiv, 変えた要素)`（widget-canvas-events.ts）で知らせ、その要素の部品だけ設定データへ読み戻す
- Widget全体の上下の余白は「Widget全体の設定」か、何も選んでいないときの枠の上下の辺で
- 「デフォルト時のコードを表示」はWidget全体のコード（見るだけ）

## 保存の形（builder-data.ts）

```html
<style>…</style>
<div class="nc nc-builder nc-xxxxxxxx" data-nocode="builder" data-nc-data="{…JSON…}" data-nc-screens="true" …>…</div>
<script>…</script>
```

- `data-nc-data` は `TemplateData` を JSON にして属性にしたもの（`& " < >` はエスケープ）。読むときは戻す
- 画像・動画は data URL のまま JSON に入る。保存のとき本文と同じ規則で別ファイル（`/uploads/<ハッシュ>.<拡張子>`）に置き換わるので、
  開き直したときは `/uploads/…` の形になる。`kit.ts` の `safeImage` / `safeVideo` はこの形も受け付ける
- 見本の部品の中身（html）もそのまま JSON に入る
- 「部品を解除」は無い（第3弾で HTMLモードを無くした）。コードを直したいときは見本の部品の「コードで直す」
- 部品の一覧の「全部消して作り直す」で白紙に戻せる（Widget全体の設定はそのまま）。白紙になると「何から作りますか？」がまた出る

## 入口（全部この画面）

| 押すもの | 開き方 |
|---|---|
| ライブラリ「+ ノーコードで作る」 | ライブラリを閉じて、**白紙**（画面①・部品なし）で開く。右に「何から作りますか？」＝アンケートの例／ライブラリの見本／型8種（2026-09-24・以前は毎回アンケートの例が入り、選べず消しにくかった） |
| 見本カード「画面を作って使う」 | ライブラリを閉じて、その見本を部品にして開く（設問①②③は画面①②③に分ける） |
| LPの中のWidgetをクリック／設置済みWidgetのカード | 設定データがあればそれで、無ければ見本の部品1つとして開く |
| 部品モードの「見本」を足す・「見本を選ぶ」 | この画面を隠してライブラリを開き、「追加」を押した見本を受け取って戻る（`openWidgetLibraryForPick`） |

## 触るときの決まり

- 左の見たまま画面の中身（contentDiv）は保存しない（保存は設定データからの書き出し）ので、編集専用の目印を付けてよい。
  ただし見本の部品の中身は DOM から読み戻す（innerHTML）ので、見本の中には目印を付けない（KB 2026-09-22-001）
- 選択枠・操作ボタンは contentDiv の**外の層**に置く（selection-layer.ts・item-toolbar.ts）
- 左の文字を打ち直したときは、その部品だけ読み戻す（`syncCanvasBlock`）。部品をまたぐ入力・打てない部品への入力は `beforeinput` で止める
- 部品の数え方は「画面をまたいで上から順の通し番号」（`nc-b-12`）。書き出し（builder.ts）と読み戻し（`blockPathAt`）で同じ数え方にする
- テストは純粋ロジックに切り出して vitest で固定する（builder-data・canvas-sync は linkedom）。画面そのものはブラウザで確かめる
- スマホは `mobile-css.ts` の `[data-widget-*]`（左右→上下）と `[data-nc-tab]`（入力欄の大きさ）がそのまま効く

## Canva 風の直接操作（第2弾・2026-09-23）

- **選択枠のつまみ**（selection-layer.ts）: 右の辺＝幅、下の辺＝高さ、右下の角＝文字の大きさ、上下の辺＝Widget全体の上下の余白。
  動かしている間は見た目だけ（`preview`）、離したら確定（`commit`）。値の計算は drag-math.ts（`dragValue`）
  - 部品: 画像・図形＝幅%、余白＝高さpx、見出し・文章＝文字の大きさpx、何も選んでいないとき＝Widget全体（上下の余白）。確定で設定データへ書いて描き直す
  - 見本の部品の中の要素: そのカードにある `width`/`height`/`font-size`（CSS変数は名前で見分ける `handleKindOf`）。画像・動画は幅%の直書き
- **数字の欄**（number-scrub.ts）: 名前や単位を左右にドラッグで増減（4pxで1刻み）＋下にスライダー（範囲は `sliderRange`）。部品の入力欄（form-controls.ts）と要素ごとのカード（widget-design-panel.ts）の両方
- **部品の並べ替え**: 右の一覧の頭をつかんでドラッグ（上半分に落とすと前、下半分で後ろ。`moveTo`）
- 見本の部品の「見た目の細かい設定」は、**左で押した要素のカードだけ**を出す。「全部のカードを見る」で一覧
- 大きさの選び（見出し 大/中/小・文章 標準/小さめ・余白 小/中/大・上下の余白 狭い/普通/広い）は**数（px）**に変えた。古い中身の文字（'l' など）は `legacy` の表で読み替える（`sizeOf`・number欄の `legacy`）

## 部品の幅と置く位置（2026-09-24）

本人「動画もサイズ調整。どの部品も中央揃え・左右へ・サイズを変えられるように。ツールバーからも、選んだら必ず、枠にカーソルを当ててシームレスに動かせるように」。

- **どの部品も**（余白以外）右の欄のいちばん上に「幅（10〜100%）」と「置く位置（左・中央・右の絵）」。幅の欄の名前は部品ごとに違う（`widthKeyOf`: 画像・動画＝width、図形＝size、ほか＝boxWidth）
- 書き出し（`layoutCss`）: 画像・動画は中の絵、図形はそれ自体、ほかは外枠に `width` と左右の `margin`。外枠に効かせる部品は幅100%なら何も書かない（見本の左右いっぱいを変えない）
- 見出し・文章の「文字の寄せ」に右を足した（置く位置とは別。文字の並びの寄せ）
- 左の見たまま画面（selection-layer.ts）
  - **マウスを乗せる**と薄い点線の枠と名前（どれを触るか押す前に分かる）
  - **幅のつまみ**: 右の辺（右に置いた部品は左の辺）。中央に置いた部品は両側へ広がるので手の動きの2倍で幅が変わる
  - **つかみ所**（枠の上のまん中の十字）: つかんで左右＝置く位置、上下＝並び順（入る所に青い線）。部品は手について動く（補助線の節）
  - 画像・動画・図形・余白・区切り線・型の部品は、**部品そのものをつかんで**も動かせる（5px動いてから動き出す。離した直後の click は選び直しに使わない）
- 上のツールバー: 「配置 ⌄」＝左・中央・右を選ぶ小窓（align-menu.ts。見出し・文章は文字の寄せ、ほかは置く位置、今の値は青。見本の中・何も選んでいないときは選んだ文字の寄せ＝両端そろえも）。「サイズ」＝選んだ部品の幅と置く位置の小窓（size-popover.ts）。見本の中・何も選んでいないときは今までどおりの動き
- ⚠️ 動かしている間だけ要素に直に当てた見た目（幅・余白など）は、描き直すたびに必ず外す（`clearPreviews`）。CSSだけの変化では要素を入れ替えないので、外さないと古い見た目が勝ち続ける

## 補助線と吸い付き（2026-09-24）

本人「補助線もやって」（手で動かしたとき、どこへ収まるかが事前に見えるように）。計算は drag-math.ts（DOMなし・tests/drag-math.test.ts）、線と影は selection-layer.ts。

- **部品を動かす**: 部品は手についてそのまま動く（見た目だけ `transform: translate`）。左・中央・右に 8px（`SNAP_PX`）まで近づくと吸い付き、**ピンクの縦線**（左の端・まん中・右の端）。吸い付いていないときは、離したら収まる所（一番近い位置）を**点線の影**で見せる。離すと設定データの `place` に書く（保存できるのは3つだけなので、途中の位置には止まらない）
  - 幅が100%の部品は横に動かない。並び順は**動かしている部品の真ん中**がほかの部品の真ん中を越えたかで決める（手の位置で見ると、つかみ所が部品の上の辺にあるので、上の部品が低いと動かす前から「ここへ移す」になった）
  - 並びを変えるときは影を出さない（入る所は青い線で見せる）
- **幅のつまみ**: ほかの部品の左右の端に辺がそろう幅と、よく使う幅（25・50・75・100%）に吸い付き、自分の辺（中央に置いた部品は両側）にピンクの縦線。吸い付き先は動かし始めたときに1回だけ測る（`SelectionHandle.snaps`）
- 幅%の基準は親の中身（padding の内側・`contentBoxOf`）。つまみの手の動きの換算もこれに合わせた
- **Alt（Mac は option）を押しながら**動かすと吸い付かない（`snapThreshold(true)`＝0。本人「やる」2026-09-24）。50%のすぐ隣の49%などをつまみで作るため。ぴったり重なったときだけ線は出る。案内はつかみ所・幅のつまみの title
- 線・影は動かし終わったら selection-layer が必ず消す。`transform` も `clearPreviews` の対象

## 書式ツールバーの見た目（2026-09-24）

本人が見せたデザイン（ボタンの並びは今までと同じで、見た目だけ変えたもの）に合わせた。見た目は widget-toolbar-css.ts（クラス `wtb*`）。

- 角の丸い白い帯（細い枠・薄い影・上下左右に少し余白）。元に戻す・やり直すは丸い灰色、フォントと文字の大きさ（−／数字／＋を1つの枠）は枠つき、書式のボタンは地なし（乗せると灰色）、区切りの縦線、右端に注意書き
- 「配置」は下向きの印つき（押すと小窓。以前は押すたびに左→中央→右と回っていた）。文字色・背景色は同じ「A」で、下のバーが赤・黄
- 幅が足りないとき（帯の中身が1,110px未満）は、注意書きを区切り線なしで右寄せの次の行へ（`@container`。区切り線だけが行の頭に残らないように）
- スマホは mobile-css.ts で画面の端から端まで（角丸・影・左右の余白を外す）・1行の横スクロール・注意書きは出さない（前から）
- ダークでは周りと一緒に暗くなる（下の「ダーク表示」。以前は白のままにしていた）

## ダーク表示（2026-09-24・本人の決定「周りだけ暗く・Widgetは白」）

周り（ヘッダー・地・右の欄・書式ツールバー・小窓）はアプリの他の画面と同じ規則で暗くし、まん中の見たまま画面（LPに入る中身）は配信されるLPと同じ白地・黒い文字のまま。ライトの見た目は変えていない（ライトで Widget編集の全要素の色・枠・影・大きさを変更前と突き合わせて一致）。

- 周りのインラインstyleの色は `var(--sb-c-xxxxxx, #元の色)`（定義は src/index.html。ダークの値は src/shared/dark-color.ts と同じ規則）。注入した `<style>`（ツールバー・右の欄・仕切り）は dark-runtime-css.ts が自動で暗くする
- 見たまま画面: LPの枠は地を透かして 620px の箱を白い紙にする（`frameStyle`）。ポップアップの枠の地（離脱防止＝灰色・追従型の角＝白）は配信の見え方なのでダークでも変えない
- ⚠️ プレビューのCSSとWidget自身の `<style>` は、`data-dark-runtime="skip"`（styleTag と contentDiv に付ける。祖先に付いていても効く `isRuntimeDarkSkipped`）で実行時の上書きの対象から外す。外さないと Widget の白い地が黒く塗られる
- ⚠️ contentDiv には `color:#000;color-scheme:light`。ダークの html は `color-scheme:dark` なので、何もしないと Widget の文字（色の指定が無いところ）が白くなって白い紙に消える
- 選んだ行・タイルの地 `--sb-accent-tint` は、ダークでは theme-color.ts の `darkTint`（ライト用の薄い地を同じ規則で暗くしたもの）。ライト用の薄い青のままだと明るくなった文字が読めなかった。右の欄の赤い文字（消す・エラー・全部消して作り直す）はダークだけ #FF8A7A（nocode-form-css.ts）
- ダークでも白のままでよいもの: 選択枠のつまみ・つかみ所（白い紙の上）、「デフォルト時のコードを表示」のスイッチのつまみ、色見本（実際の色）
- 確かめ方: ブラウザで、周りの全要素について文字と地のコントラスト比を計算して 3 未満が無いこと、プレビューの中の全要素の色がライトとダークで同じこと（tests/widget-studio-dark.test.ts はソースで固定）

## ポップアップの中身もこの画面で（2026-09-24）

本人「離脱防止ポップも同じ感じで簡単に。画面が色々あると分かりづらいから、Widget編集と同じ画面を移植」。決定: **中身だけWidget編集で**（名前・配信・表示条件・位置・出し分けの画面はそのまま）・**追従型も同じ**。

- 入口: ポップアップの編集画面の左上＝今の中身の小さな絵（iframe・スクリプトは動かさない）＋「中身を編集」（popup-content-card.ts）。一覧のカードの絵も今の中身（以前はプリセットの絵のまま＝直した中身が一覧に出なかった）
- 画面の組み立ては `mountStudio`（widget-studio.ts）で LPのWidget と共通。ポップアップ用は popup-studio.ts の `openPopupStudio`: 右上＝「ポップアップに反映」（書き出したHTMLを保存・保存できたら閉じる）
- 見たまま画面の枠（`PreviewFrame`）: 離脱防止＝`overlay`（灰色の地＝配信の暗幕、最大500pxで中身の幅の箱＝配信の .ep-content）／追従型の上下の帯＝`lp`（620px）／追従型の角＝`corner`（中身の幅）
- 始まり（popup-studio-start.ts・DOMなし）: 空＝白紙／設定データつき＝そのまま／プリセット・手書き＝見本の部品1つ（追従型の CSS も中の `<style>` へ。保存したら CSS の欄は空にする＝二重に効かない）
- ライブラリの見本・作成したWidget・お気に入りも部品に入れられる（2026-09-24 本人「やって」）。ポップアップの画面にはLPの編集＝Quill が無いので、ライブラリは `openWidgetLibraryForPick(null, …)`＝**見本を選ぶだけ**で開く: 「+ Widgetを作成」「+ ノーコードで作る」「画面を作って使う」（LPへ入れる入口）は出さない。「追加」は必ず部品として渡る
- 消したもの: 離脱防止の「デザイン」タブ（要素ごとのカード＝見本の部品の「見た目の細かい設定」に同じものがある）と、HTMLの欄。「コード」タブに JavaScript・HeadTag・BodyTag（追従型は CSS・JavaScript）だけ残した
- 設定画面の「プレビュー」（離脱防止 `previewPopup`・追従型 `previewFollowPopup`）も、画面に載せてから中身の `<script>` を動かす（`runWidgetScripts`。innerHTML では動かず「次へ」で画面②へ移れなかった）。順番は配信と同じ＝中身のスクリプト→ポップアップの JavaScript。小さな絵は `<script>` を外して描く（`thumbnailHtml`。絵の iframe はスクリプトを動かさないので、残すとエラーが出続ける）
- ⚠️ 配信（delivery-popup-html.ts）: 中身の `[data-nc-go]`（部品で作った「次の画面へ」）を押したときは、ポップアップの「LPに戻る＝閉じる」「遷移先へ移動」より先に抜ける（入れる前は、画面①→②で閉じてしまった）
- 幅・置く位置の欄は、値が無い部品（見本の部品・以前の中身）でも書き出しの既定（100%・中央）を見せる（`fallback`。以前は空欄と「左」に見えていた）

## 画面の作り直し：3列（2026-09-24・本人の決定 C-1a）

本人「別ページにしたことで画面がダサい」→ デザイン案（別に作ったモック）で比べて、**C-1a＋元々の上のツールバー＋ドラッグ&ドロップで部品を足す** に決定。

- ヘッダー: 「← LPに戻る」（ポップアップは「ポップアップの設定へ」）・名前（ポップアップは印「離脱防止／追従型」と名前）・右に「作成したWidgetに登録」「更新する／LPに入れる／ポップアップに反映」
- **左の列**（`[data-widget-pane="parts"]` 320px）: 画面①②…（上）・**並び**（部品の頭だけ。押すと選ぶ・つかんで並べ替え・選んだ行に複製↑↓消す（「複製」「消す」は横書き）。いちばん下に「Widget全体の設定」）・**部品を足す**（よく使う11個＋「もっと見る」→ その下に**まとまった型**（2列）→「ライブラリの見本から選ぶ」＝palette.ts。下の「部品を増やした」）。template-form.ts の `partsHost` で出す
- **部品を足すはドラッグ&ドロップ**: タイルをつかんで見たまま画面へ運ぶと、入る所に青い線（部品の真ん中より上か下か）→ 離すとそこに入って選ばれる（`NC_BLOCK_MIME`・widget-studio-builder.ts が editorBody で受け取り `form.insertBlock`）。押すだけなら、いちばん下
- **まん中**: 元々の書式ツールバー（1行に収まるよう間隔を少し詰めた）＋灰色に点の地（`--sb-c-f0f1f4`）に白い紙（620px・影）
- **右**（360px。仕切りで幅を変えられる）: 部品を選んでいればその設定を**段に分けて畳める**＝レイアウト（出す画面・幅・置く位置）／中身／押したとき（`Field.section`: layout・press。無ければ中身）。選んでいなければ画面の知らせと設定・Widget全体の設定（開いておく）。白紙なら「何から作りますか？」（1列）
- 空の見出し・文章・画像・動画は高さ0で見えなかった（ドラッグで入れても分からない）→ 見たまま画面のプレビューCSSにだけ薄い字「見出しを入れてください」・灰色の箱「画像を選んでください」を足す（`EMPTY_PLACEHOLDER_CSS`。保存する中身には入らない）
- スマホ: 見たまま画面（42%）→ 部品の列（28%・1つの箱で送る）→ 設定の順に積む（`order`）。ヘッダーの戻るは矢印だけ、名前は縮める
- **ポップアップ「LPの上に重ねて見る」**（本人「必ず実装したい」）: 離脱防止の中身を開くと、見たまま画面の後ろにそのABテストのLP（配信の割合がいちばん大きいVersion）を `/preview/:versionUid?bare=1`（検証用の帯・ポップアップ抜き）で敷き、配信と同じ暗い幕（rgba(0,0,0,.4)）を重ねる。左上で「LPの上に重ねて見る／中身だけ」（popup-underlay.ts）。LPが無ければ切り替えは出さない
  - ⚠️ iframe の sandbox は `allow-scripts allow-same-origin`。`allow-scripts` だけ（別の出どころ扱い）だと、ブラウザによっては読み込みを止める（実測 ERR_BLOCKED_BY_CLIENT）
  - ⚠️ ローカルのモック（4010）は tsx の常駐で自動では読み直さない。サーバー側を直したら確認は別ポート（launch.json の lp-sakusei-verify-mock 4011 ＋ lp-sakusei-verify 5180）で。4010 を再起動するとローカルのテストLPが消える
- **PC／スマホ**（ヘッダーのまん中・2026-09-24 本人「続けて作って」）: スマホは見たまま画面を幅375px（`SP_WIDTH`）にし、@media もその幅で判定する（`widgetPreviewCss(css, scope, width)` → `scopeWidgetCss` → `mediaAtLpWidth(…, width)`。**stripSbPreviewCss は変えていない**＝幅を渡しただけ）。離脱防止の箱は配信と同じ min(500px, 92vw)、後ろのLPも375pxにする。スマホの画面では切り替えを出さない
- **追従型も「LPの上に重ねて見る」**: 暗い幕なし・配信で出る所（上の帯＝上、下の帯＝下、右下・左下）に置く（`UnderlayStyle.place`）。LPを敷いている間は白い紙（地・影・最低の高さ）を消す（帯の下に白い余りが出ていた）。離脱防止は縦もまん中（配信と同じ）
- 空の「画像と文章」も、画像の欄を灰色の箱・文章を薄い字で見せる

## 部品を増やした・移行先・もっと見る（2026-09-24）

本人「部品を足すの部品の数を増やして。必ず『移行先』を作る（ボタンやすでにあるウィジェットに被せて使う。移る先の画面やURLを設定できる）。
ここに出すのは11個＋もっと見る。もっと見るを押したら、同じ左の列のままサムネ付きで全部」。選んでもらったこと: 被せ方＝**範囲を自由に置く**／
タップしたときの表示＝**移る先の画面／URL**／11個＝**今の10種＋移行先**（新しい部品は、もっと見るの中）。
同日あとから本人「部品とまとまった型は違う機能として考えている。消えたまとまった型の項目を復活」→ **まとまった型は、もっと見るに入れず、部品の下に別の段（2列）で出す**（ふだんも、もっと見るを開いているときも）。

- **部品を足す（palette.ts）**: 見出し・文章・ボタン・画像・移行先・画像と文章・箇条書き・図形・動画・余白・区切り線（`PALETTE_FIRST`）＋「もっと見る」。
  もっと見るは同じ場所で、全部の部品（35種）を種類ごと（文字／伝える・見せる／画像・動画／押す・移る／数字・評価／かざり・すき間＝`PALETTE_GROUPS`）にサムネで並べる。型は入れない。
  PCでは、もっと見るの間だけ左の列を 320px→496px に広げて**3列**（本人「横に広げて3列で表示」。`:has([data-ncf-catalog])`・スマホは広げない。3列の指定は2列のもとの指定より後ろに置く＝同じ強さなので後ろが勝つ）
  もっと見るを開いている間は、左の列を一覧だけにする（画面のタブと並びを畳む＝本人「上の部分が残って狭い」。nocode-form-css.ts の `:has([data-ncf-catalog])`。「戻る」で出る）。
  「戻る」でふだんへ。開き具合は部品を足して組み立て直しても残す。サムネは絵を描かず、見本の中身で本当に書き出して縮めたもの（palette-thumbs.ts。shadow root の中・スクリプトは動かさない・一度作ったら使い回す）
- **増やした部品**（builder-blocks-more.ts＝入力欄と確かめ／builder-blocks-more-render.ts＝書き出し）: 吹き出し・価格（取り消し線の通常価格・割引率は数字から）・囲み枠（淡い地／枠線／見出しを帯に）・
  注意書き（※）・表（1行に1つ・「|」で区切る）・星の評価（0.1点刻み）・バッジ・画像を並べる（2〜3枚）・ポイント・数字で見せる・開いて読む（details）・下向きの矢印（動きを減らす設定の人には止まる）。
  形の土台のCSSは、その部品を使っているWidgetにだけ1回出す（`moreBaseCss`）。文字は右の欄で直す（見たまま画面では打てない＝つかんで動かせる）
- **もっと増やした部品**（同日・本人「部品をもっと増やして」。builder-blocks-extra.ts＝入力欄と確かめ／builder-blocks-extra-render.ts＝書き出し）: 帯見出し（塗り／リボン／下線）・マーカー文章・
  特徴（アイコン10種＋文字）・口コミ（1件・星）・プロフィール（監修者）・画像に文字（暗くする濃さ・高さ）・ビフォーアフター・電話ボタン（番号の数字だけを tel:・押した数を数える）・
  クーポン券（コードは押すと全部選べる）・数字を並べる（「上の文字|数字|単位」4つまで）・ランキング（1〜3位は金銀銅）・飾りの区切り（波／ギザギザ／点々／斜め）。飾りの擬似要素は ::after（::before は空の案内）
- **移行先**（hotspot-model.ts）: 並びの中で**すぐ上の部品に被さる**透明な押せる範囲。位置と大きさはその部品に対する %（x・y・w・h）。
  書き出しでは被せた部品の中（閉じるタグの前）に `<span data-nc-go>`（画面へ）か `<a href>`（リンク）で入れ、被せた部品は `position:relative;isolation:isolate`（builder.ts）。
  読み上げは「画面②へ」「リンクを開く」。区切り線（中に入れられない）とリンクの図形にリンクの移行先は被せない（保存の前に知らせる）
  - 見たまま画面: 斜線の枠と移る先の名前（`MORE_PREVIEW_CSS`。保存する中身には入らない）。枠をつかんで動かす（左右・上下のまん中と端に吸い付く）・四辺のつまみで大きさ（widget-studio-hotspot.ts）
  - 足し方: 部品の上へドラッグで運ぶ（低い部品＝ボタン・見出しならまるごと、高い部品＝画像・見本なら落とした所に 200×64px ほど＝`dropRect`）。押すだけなら、選んでいる部品（無ければいちばん下の部品）に被せる
  - **部品を動かす・複製する・消す・別の画面へ移すと、被せた移行先も一緒に付いていく**（`moveGroupBefore`・`groupStep`・`duplicateGroup`・`removeGroup`・`moveBlockToScreen`）。移行先だけを動かすと、動かした先の上の部品に被さり直す。移行先には「この部品を出す画面」は出さない
  - 被せた部品の文字・見本の中身を見たまま画面から読み戻すときは、移行先を外して読む（canvas-sync.ts `withoutHotspots`）。移行先を押したら移行先を選ぶ（`outermostBlock`）
  - 移行先だけが入っている空の部品には、見たまま画面だけ `data-nc-empty` を付けて「画像を選んでください」などの案内を残す（`:empty` では空に見えなくなり、高さ0で移行先も見えなくなっていた）

## 押したときの「LPの上で」（2026-09-24）

本人「LP上アクションをできるように。例えば画像を表示したりクーポンみたいな」。選んでもらったこと: 画像を大きく表示・クーポンを表示・小窓で見せる（中身は画面で作る）・
LPの中の場所へ移動＋ほかにも色々（文字をコピー・動画を大きく再生・電話をかける・閉じる）。**押せる部品すべて**（ボタン・画像・図形・動画・画像と文章・移行先・押せる型）で使える。

- 右の欄の「押したとき」の下に「LPの上で」の段（template-form.ts `lpPressRows`）。上の段（画面へ移る・リンク）と同じ1つを選ぶ。小窓で見せるなら「小窓に出す画面」の段（＋新しい画面も）
- 入力欄は block-kit.ts の `actionFields`（選んだアクションの分だけ出す＝showIfItem）。データは action＋act*（actImage・actVideo・actTitle/actAmount/actCode/actNote・actScroll/actAnchor・actCopy・actTel）
- 書き出し（press-actions.ts）: 押す要素に印（`data-nc-pop`・`data-nc-modal`・`data-nc-scroll`・`data-nc-copy`・`data-nc-close`）。画像・動画・クーポンの小窓の中身は Widget の中に隠して置く（`.nc-pops`）。
  小窓で見せるは、選んだ画面を小窓へ移して出し、閉じると元の場所へ戻す。電話をかけるは `tel:` のリンク（block-kit.ts `hrefOf`）
- スクリプト `ACTIONS_SCRIPT`（使っているWidgetだけ・1回）: 押す前（capture）に受け取る。× ・背景・Esc で閉じ、開いたら × に、閉じたら押した所に入力の印を戻す。コピーは「コピーしました」を出す。
  閉じるは、入っている小窓・離脱防止ポップ（`.ep-close` を押す）・追従型（`.fp-close`）を閉じる
- 小窓は Widget の中に `position:fixed` で置く（Widgetだけに効くCSSのまま出すため）。LPの親に transform があると、その中に収まる
- 確かめ方: アプリの画面は body の高さが固定でスクロールしない・隠れたタブはクリップボードを許さない → ページの中に普通の文書（iframe）を作り、書き出したHTMLを入れて押す

## 部品「ロード中」（2026-09-24）

本人「部品にロード中という内容を追加して」。動き＝本人の選択「数秒後に次の画面へ」（診断LPの「あなたに合う内容を診断中…」）。

- 見た目: くるくる・バー（待つ時間ぶんで満ちる）・点々。下の文字・待つ時間（1〜10秒・0.5秒刻み）・色。「終わったら」＝画面②③…（＋新しい画面）かリンク（LPの上のアクションは出さない）
- 書き出し: `data-nc-wait="3000" data-nc-then="s2"`（リンクなら隠したリンク `.nc-b-loading__go`）。切り替えのスクリプト（builder.ts `SCREENS_SCRIPT`）が、その画面が出たら数え始める
  （最初の画面なら、見えたら＝IntersectionObserver）。ロード中があるWidgetは1画面でもスクリプトを入れる
- **Widget編集の見たまま画面（`[data-widget-preview]` の中）では移らない**（動きだけ見せる。移ると、タブの画面と見えている画面がずれる）
- 動きを減らす設定: くるくるはゆっくり、点々は止める。バーは進み具合を伝えるので残す

## Backspace・Delete・消しゴムで部品を消す（2026-09-24）

本人「バックスペース・デリートで消したい」「（ツールバーの消しゴム）これでも」。消しゴムは本人の選択「文字の選びで分ける」。

- **Canva式（本人の選択・同日「消えない」のあと）**: 見たまま画面で**1回押すと部品を選ぶだけ**（文字の入力の印を外す）→ Backspace・Delete で部品を消す。
  **ダブルクリックで文字を打つ**（押した所から）・**Esc で打つのをやめる**・**Enter で選んだ文字の部品の末尾から打つ**（widget-studio-delete-key.ts `attachPartKeys`）。
  打っている途中の Backspace・Delete は文字を消す。空になった部品でもう一度押すと部品ごと消す。⌘/Ctrl/Alt と一緒のときはブラウザに任せる。
  ツールバー右の案内も「文字はダブルクリックで直す」
  （以前は1回押すと文字を打てる状態になり、ボタンや見出しで Backspace を押すと文字だけが消えて「消えない」になっていた）
- 並びの行（左）で Backspace・Delete → その部品を消す。右の欄の入力欄では消さない
- 上のツールバーの消しゴム: 文字を選んでいれば今までどおり書式クリア。選んでいなければ、選んでいる部品を消す
- どれも「消す」ボタンと同じ（被せた移行先も一緒に消える・`form.removeBlock`）。部品を消したことは元に戻せない（ツールバーの元に戻すは文字の打ち直しだけ）

## 点検で見つけた抜け・食い違いの直し（2026-09-24・本人「他にあった方が良かったり反応しないもの、説明やイメージと異なるものありますか？」→「先に1〜6」）

1. **部品を選んだだけでツールバーの文字の飾りが効かない**（Canva式にした抜け）→ 文字を選んでいなければ、選んだ部品の文字全体に効かせる
   （太字・斜体・下線・取り消し線・文字色・背景色・マーカー・書体。widget-visual-editor.ts `onText`、部品の文字の入れ物＝widget-studio-toolbar.ts `partText`）
2. **元に戻す・やり直すが部品の操作に効かない** → 中身の変わり目を順に覚える（widget-studio-history.ts。続けて打った文字は1回ぶん・100回まで）。
   ツールバーの ↶↷ と ⌘Z／⌘⇧Z（Ctrl+Z／Ctrl+Y）。右の欄の入力欄の中はブラウザの戻すに任せる
3. **サイズ＋で小さくなることがある**（数字が19から始まっていた）→ 今の大きさから1pxずつ。文字を選んでいなければ、見出し・文章は部品の「文字の大きさ」、ほかは文字全体。数字も今の大きさを出す
4. **右の欄で文字を直すと見たまま画面で付けた飾りが消える** → 変わった所だけを差し替える（rich-text.ts `applyPlainEdit`。ほかの飾りは残る）
5. **部品の文字の中のリンク・画像はあとで消える**（部品の文字はリンク・画像を持てない）→ 部品を選んでいるとき、リンクは「押したとき」をリンクにする
   （見出し・文章など押せない部品には、リンクを開く移行先を被せる）。画像は画像の部品として下に足す。見本の部品の中は今までどおり
6. **文字の部品は本体をつかんで動かせない** → どの部品も本体をつかんで動かせる（打っている途中の部品と見本の部品は除く）

あわせて見つけたこと: **見たまま画面にアプリのCSSが漏れていた**（取り込んだ管理画面のCSSの `i { margin-right:10px; font-size:30px }`・`label { display:block }`）。
斜体の文字が30pxに見え、下向きの矢印・ロード中の点々もずれていた（配信のLPでは起きない）→ 見たまま画面のCSSの先頭で標準に戻す（widget-style-scope.ts `PREVIEW_LEAK_RESET`）

