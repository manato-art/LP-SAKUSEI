# フロー総点検（指示⑮）— 行き止まり撲滅ログ

> 目的: 実操作フローを辿り「クリックできるのに実装できてません／空／未反映」を全部潰す。
> 方針: 1個ずつ 精査→作成→精査。実物と照合し、足りなければ導入。API要件は明示して実証可能な形に。
> 許可された実本体の経路: ページ→金谷→ナイトエンペラー→複製用→バージョン（エディタ配下）。

## バックログ（dead-end 一覧・grep由来）

### エディタ配下（最優先）
- [x] `version-dots-menu` 別のbeyondページに複製 … ✅ 実装（採取モーダル＋複製先選択＋duplicate_to）
- [x] `version-dots-menu` 選択してアーカイブする … ✅ 実装（チェックボックス選択＋一括アーカイブ）
- [x] `version-actions` アーカイブ一覧 … ✅ 実装（Version▼にアーカイブモード＋復元）
- [x] `step-add-modal` ステップの作成 … ✅ 実装（addArticle＋作成→新ステップ移動＋`< >`ステップ移動）
- [ ] `widget-manager` 未実装×2 → Widget管理の操作
- [ ] `widget-library` プレビュー … 未採取
- [ ] `link-replace` 離脱防止ポップアップリンク置換 … 未採取
- [ ] `toolbar/link-dropdown` 成果単価通貨 / ページ内移動 … 未採取
- [x] `redirect-pages` 中間ページ追加 … ✅ 実装（採取した詳細画面＋作成/一覧/設定フォーム/保存）
- [ ] `exit-popup` 操作 … 未採取
- [x] `basic-info` パラメータ付きURL … ✅ 実装（採取した入稿URLビルダー＋生成）
- [x] `basic-info` 選択肢一覧（配信タイプ/CV条件/媒体/性別/年齢） … ✅ 実装（ドロップダウン＋保存反映）

### サイドバー/その他
- [ ] `folders` ラベル操作 / フォルダ操作メニュー … 未実装
- [ ] `tasks` タスク作成 … 未実装
- [ ] `addon` トライアル申込 … 未実装（課金系）
- [ ] `tool-pages` ツール操作 … 未実装
- [ ] `main.renderNotBuilt` … 未ルートのパスは「まだ作っていません」
- [ ] サイドバー各行の遷移先が空/スタブな画面（dashboard/conversions/domains/report-exclusions/seminar/rankings 等）

## 進捗ログ
（着手・完了をここに追記していく）
