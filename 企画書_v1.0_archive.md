# SquadBeyond 完全再現クローン 企画書（バグ発見用サンドボックス）

- 版: v1.0
- 作成日: 2026-08-31
- 作成: 企画担当AI（フェイブル）
- 想定読者: 実装担当AI（本会話を一切見ていない前提。この文書だけで実装できるように書いてある）
- 再現対象: `https://app.squadbeyond.com/`（御社子会社が制作したLP A/Bテスト運用SaaS「Squad beyond」）

---

## 0. この企画書の読み方（実装AIへの絶対前提）

- **あなた（実装AI）は、この文書に書かれていること以外の会話履歴を持っていない前提で書かれている。** 不明点を「たぶんこうだろう」で埋めてはいけない。曖昧な箇所に当たったら、手を止めて §12「未確定事項」を確認し、選択肢を提示して人間に聞くこと。
- **この案件は「再現（reproduction）」であって「改善」ではない。** 見た目・挙動・遷移は**オリジナルに忠実**であることが最優先。あなたの美的判断でUIを「良く」しないこと。社内の一般的UI規約（余白ゼロ・絵文字禁止・カード型メニュー等）は**新規プロダクト向けの規約であり、本案件では忠実再現がそれらに優先する**（§9で明記）。
- 用語:
  - **beyondページ / ab_test**: A/Bテストの器。1つのLP施策の単位。URLは `/ab_tests/:uid/...`。
  - **記事 / article**: beyondページ内のコンテンツページ。
  - **バージョン / version**: 記事のバリアント（A/B比較される個々の版）。エディタで編集する実体。
  - **フォルダ / folder**: beyondページを束ねる整理単位。
  - **媒体 / media**: 流入元（Facebook, LINE, TikTok等）。広告連携の対象。
  - **CV**: コンバージョン。
  - **モックAPI / モックバックエンド**: 本番サーバーの代わりに、合成データを同じ形で返す偽サーバー（§6）。

---

## 1. Step 0 ―― 最重要・最大リスク（着手前に必ず読む）

この案件で事故ると致命的なポイントを、実装より前に確定する。**ここを外すと全部作り直しになる。**

### 1-1. 【最大リスク】本番の実データを絶対にクローンへ持ち込まない
オリジナルは**親会社グループの機密情報の塊**である。デベロッパーツールで観測すると、実際の売上・配信金額・粗利・ROAS、実在するLP施策名、**実在する従業員の氏名**、独自ドメイン等が見える。

- **クローンには一切の実データをコピーしない。** ページタイトル・人名・金額・ドメイン等はすべて**架空の合成データ（フィクスチャ）**に置き換える。
- デベロッパーツールで採取してよいのは「**データの構造（フィールド名・型・階層）**」と「**見た目（レイアウト・CSS・コンポーネント構成）**」だけ。値そのものは持ち込まない。
- 合成データの人名は「テスト太郎」「サンプル花子」等、明らかにダミーと分かる名前にする。金額・数値はランダムなダミー。
- 本企画書自身にも実データの値は記載していない（構造のみ）。同じ原則を実装物にも徹底する。

### 1-2. 【最大リスク】クローンを本番APIに絶対に接続しない
クローンは**完全に隔離**する。`api.squadbeyond.com` / `api-workers.squadbeyond.com` / `report.squadbeyond.com` へ実際にリクエストを飛ばすコードを一行も書かない。データは§6のローカルモックサーバーからのみ供給する。

- 誤って本番を叩くと、認証情報の漏洩・本番データの改変・利用規約違反のリスク。
- クローンのAPIベースURLは**必ず** `http://localhost:xxxx` 等のローカルに固定。本番ドメインをコードにハードコードしない（テストで検知する。§8）。

### 1-3. 【スコープ確定】今回作るもの・作らないもの
人間との確認の結果、以下で確定している。

| 項目 | 確定内容 |
|---|---|
| 再現方式 | **ブラックボックス再現**（ソースコードは入手不可。デベロッパーツールで外から観測して作り直す） |
| 対象範囲 | **全ページ（全82ルート）を触って動く状態で再現** |
| 完成イメージ | クリックで画面遷移でき、一覧・詳細・フォーム操作・タブ切替などが**モックデータ上で**動く |
| 技術ベース | **HTMLベース**（ブラウザで開ける実HTML/CSS/JSのWebアプリ）を**Claude Code（CLI）で構築** |
| 参照方法 | 実サイトをデベロッパーツールで観測しながら1ページずつ再現 |
| いったん作らないもの | 「バグ発見/バグ建築ツール」化は**後回し**（まず"触れる完全再現"を作る）。バックエンドの集計ロジック（売上/ROASの計算）やDBは**再現しない**（モックが固定値/擬似計算で返す） |
| 公開範囲 | **社内限定・非公開**。GitHubにあげる場合も**private**。子会社プロダクトの複製物なので外部公開しない |

### 1-4. 【スコープ管理】82ページを一気に作らない
全ルートを同時並行で作ると破綻する。§4の優先度（Tier1→Tier4）に従い、**共通土台（レイアウト・デザイントークン・モックサーバー）→ 主要ページ → 周辺ページ**の順で、1ページずつ「観測→再現→照合」を回す（§3の標準手順）。

---

## 2. 再現対象の実態（2026-08-31 デベロッパーツール実測結果）

実装の前提となる、オリジナルの技術構成。すべて実測値。

### 2-1. フロントエンド
| 項目 | 実測値 |
|---|---|
| 種別 | React 製 SPA（シングルページアプリ） |
| ルーティング | React Router（`__reactRouterVersion` をwindowに確認） |
| ビルド | Vite（`/assets/index-<hash>.js`, `/assets/index-<hash>.css` のハッシュ付き成果物） |
| メインJSバンドル | 約 **5.8MB**（難読化済み。中身のロジックは読めない＝ブラックボックスである根拠） |
| スタイル | **Emotion（CSS-in-JS）**。クラス名は `css-1v797yu` `eey6hzc7` 等の動的ハッシュ。加えて `app.css` `normalize.css` |
| アイコン | **Material Icons**（Googleフォント）＋ **Font Awesome 5.15.4**（CDN） |
| 日付ピッカー | **Litepicker**（`--litepicker-*` CSS変数を確認。ダッシュボードの期間選択） |
| チャート | **Recharts**（`.recharts-wrapper`/`.recharts-surface` をDOMに確認。SVG描画） |
| エラー監視 | Sentry（`@sentry/react` 10.44.0） |
| ルート要素 | `<div id="root">` |
| 言語 | `<html lang="ja">`、フォント `"Hiragino Sans", sans-serif` |

### 2-2. バックエンド / API構成
| ドメイン | 役割 |
|---|---|
| `api.squadbeyond.com/api/v1/*` | メインREST API。Rails/Devise系（`/users/sign_up` `/users/confirmation` `/users/password/edit` 等の規約的ルートから推定）。バージョニング `v1`、共通クエリ `?per_page=&locale=ja&referer=` |
| `api-workers.squadbeyond.com/api/v1/*` | 重い集計系の別サービス（`teams/versions` `teams/version-rankings` 等） |
| `report.squadbeyond.com` | ランキングの別ドメイン（サイドバー「ランキング」の外部リンク先） |
| WebSocket `/cable` | ActionCable（Rails）。リアルタイム更新（CV速報）に使用 |

観測できた実エンドポイント（レスポンス200確認済み）:
- `GET /api/v1/ab_tests?per_page=20`
- `GET /api/v1/ab_tests/rankings?start_date=&end_date=&sort=sales&sort_direction=desc&per_page=5`
- `GET /api/v1/articles?per_page=20`
- `GET /api/v1/teams/versions?per_page=20`（workers）
- `GET /api/v1/teams/version-rankings?start_date=&end_date=&sort_by=sales&sort_order=desc&per_page=5`（workers）

### 2-3. 外部連携タグ（クローンでは入れない）
Channel.io（サポートチャット）, Pendo（プロダクト分析）, HubSpot（各種）, Google Tag Manager / GA4, Mixpanel。
**→ クローンにはこれら外部SaaSタグを入れない**（本番の計測に混入させない・不要）。UI上でチャットバブル等の"見た目"だけ必要なら静的なダミーで置く。

### 2-4. デザイントークン（実測。§6のCSS変数として定義する）
| トークン | 実測値 |
|---|---|
| ブランド/アクセント色（プライマリ） | **`#0091FF`**（`rgb(0,145,255)`。アクティブボタン・リンク・強調） |
| プライマリ濃色（hover等） | `#0074CC`（`rgb(0,116,204)`） |
| ページ背景 | `#ECECEC`（`rgb(236,236,236)`） |
| 中立ボタン/薄い面 | `#F4F4F4`（`rgb(244,244,244)`） |
| テキスト主色 | `#000000` / `#151515` |
| テキスト副色（ミュート） | `#808080` |
| カード/コンテンツ面 | `#FFFFFF`（白） |
| フォント | `"Hiragino Sans", sans-serif` |
| アイコン | Material Icons + Font Awesome 5.15.4 |

> 注: 上記はダッシュボードでの実測。各ページで色の追加採取が要るため、§3の手順で個別に確定する。

---

## 3. 1ページを再現する標準手順（観測駆動リプロダクション）

全82ページはこの手順を繰り返して作る。**憶測でUIを作らない。必ず実サイトを観測してから作る。**

各ページについて、実装者（人間 or Claude in Chrome拡張を持つAI）は以下を行う:

1. **観測（Capture）** ―― 実サイトの対象ページをデベロッパーツールで開き、次を採取:
   - **DOM構造**: 主要な領域（ヘッダ/サイドバー/コンテンツ/テーブル/フォーム/モーダル）の階層。
   - **CSS**: レイアウト（flex/grid、幅、余白）、色、フォントサイズ、角丸、影、境界線。Emotionのハッシュ名は無視し、**算出済みスタイル（computed style）の値**を採る。
   - **API**: Networkタブで、そのページが叩くエンドポイント・クエリ・**レスポンスのJSON構造（フィールド名と型）**を採取。→ §6のモックへ反映。
   - **状態/インタラクション**: タブ、ソート、ページネーション、フィルタ、モーダル開閉、フォームバリデーション等、"触れる"要素を列挙。
   - **空/エラー/ローディング状態**: 可能なら空データ・読み込み中の見た目も採取。
2. **合成データ化（Synthesize）** ―― 採取したJSON構造を元に、**実値を全て架空値に置換**したフィクスチャを作る（§1-1厳守）。
3. **再現（Build）** ―― §5の共通コンポーネントを組み合わせ、当該ページのHTML/CSS/JSを実装。モックAPIからデータを流し込み、インタラクションを実装。
4. **照合（Diff）** ―― 実サイトとクローンを並べ、レイアウト崩れ・色ズレ・遷移不一致を潰す。§8の受け入れ条件でチェック。
5. **記録（Log）** ―― `docs/pages/<route>.md` に「採取した構造・エンドポイント・再現状況・未対応点」を残す。次のページや引き継ぎで使う。

> ブラックボックス再現の限界: サーバー側でしか分からない挙動（正確な集計値、権限による分岐の全パターン、エラーコード網羅）は観測しきれない。**見えない挙動は"それらしい擬似挙動"で埋め、`docs/pages/*.md` に「推測」と明記する**（症状糊塗ではなく、限界の明示）。

---

## 4. 全ページインベントリ（全82ルート）と再現優先度

React Routerのルート定義をバンドルから抽出した**全82ルート**。サイドバー項目との対応と、再現優先度（Tier）を付す。

### サイドバー ⇔ ルート対応
| サイドバー | 主ルート |
|---|---|
| ダッシュボード | `/dashboard`, `/dashboard/:uid` |
| AI | `/sb_ai`, `/sb_ai/chat`, `/sb_ai/chat/:conversationId` |
| タスク | `/tasks` |
| ページ | `/folders`（フォルダ木＋beyondページ一覧）ほかフォルダ/ab_test系 |
| CV速報 | `/conversions`（ActionCableでリアルタイム） |
| ツール | `/articles/bulk_replaces` 等の一括系 |
| 外部連携 | `/teams/ad_accounts`, `/teams/asp_accounts`, `/redirections/ad_accounts/*` |
| ドメイン | `/teams/domains` |
| 拡張機能 | `/addon/option-list` |
| レポート除外 | `/report-exclusions` |
| イベント・セミナー | `/seminar` |
| ランキング | 外部 `report.squadbeyond.com`（別プロジェクト。クローンでは静的リンク or 簡易再現） |

### Tier 1（土台・最優先。これが無いと他が作れない）
- 共通レイアウト（左サイドバー＋ヘッダ＋コンテンツ）… 全ページ共通シェル
- `/` （ルート。ログイン後ダッシュボードへ）
- `/dashboard`, `/dashboard/:uid` … KPIカード（売上/配信金額/粗利/PV/CLICK/CV/ROAS/ROI）＋Recharts折れ線＋各種一覧（新規beyondページ/新規Version/ランキング）
- `/folders` … フォルダ木＋beyondページ一覧（アプリの中核ナビ）
- 認証系: `/users/sign_up`, `/users/confirmation`, `/users/confirmation/code/new`, `/users/forgot_password`, `/users/password/edit`

### Tier 2（中核機能。beyondページ運用の主要導線）
- `/ab_tests/:uid/articles` … 記事/バージョン一覧
- `/folders/:folder_uid/ab_tests/:ab_test_uid/edit` … **LP/HTMLエディタ**（最難関。§5-1）
- `/ab_tests/:uid/articles/:article_uid/previews` … プレビュー
- `/ab_tests/:uid/reports`, `/ab_tests/:uid/reports/lp`, `/ab_tests/:uid/reports/swipe` … レポート
- `/ab_tests/:uid/creative_report` … クリエイティブレポート
- `/ab_tests/:uid/versions/swipe` … スワイプ（バージョン横断）
- `/ab_tests/:uid/articles/exit_popups` … 離脱ポップアップ
- `/ab_tests/:uid/redirect_pages`, `/folders/:folder_uid/ab_tests/:ab_test_uid/redirect_pages` … リダイレクトページ
- `/ab_tests/:uid/articles/split_test_settings/{devices,oses,carriers,hours,periods,params}` … スプリットテスト条件設定（6種）
- `/ab_tests/:uid/articles/htmls/heatmaps/comparisons` … **ヒートマップ比較**（§5-4）
- `/ab_tests/:uid/options/devide`
- `/conversions` … **CV速報（リアルタイム。§5-3）**
- `/conversion-reports` … CVレポート
- `/tasks` … タスク管理

### Tier 3（設定・チーム・連携）
- フォルダ配下: `/folders/:uid/edit`, `/folders/forms`, `/folders/:folder_uid/forms/:uid`, `/folders/:folder_uid/conversion_tags`, `/folders/:folder_uid/operator_articles`, `/folders/:folder_uid/operator_articles/:uid/edit`
- チーム: `/teams/ad_accounts`, `/teams/asp_accounts`, `/teams/domains`, `/teams/tags`, `/teams/product_search_forms`, `/teams/plans`, `/teams/:team_uid/members/:member_uid/invitations/edit`, `/users/teams`
- 広告OAuthコールバック: `/redirections/ad_accounts/{facebook,google,microsoft,x,yahoo}`, `/redirections/ab_tests/google_postback_setting`
- 審査: `/inspections`, `/inspections/authorities`, `/inspections/folders`
- 通知設定: `/settings/internal_notifications/{member,team}`
- ユーザー: `/users/edit`, `/users/public_api_key`
- 一括系: `/articles/bulk_replaces`
- AI: `/sb_ai`, `/sb_ai/chat`, `/sb_ai/chat/:conversationId`
- レポート除外: `/report-exclusions`
- 課金: `/teams/plans`, `/plans/:planUid/payment/checkout`, `/addon/option-list`

### Tier 4（管理者・周辺・低頻度）
- 管理者: `/admin-report`, `/admin/articles/html_parts`, `/admin/plans`, `/admin/plans/new`, `/admin/preset_access_denials`, `/admin/product_search_forms`, `/admin/teams`, `/admin/teams/new`, `/admin/teams/:team_uid/members`, `/admin/teams/:team_uid/plans/payments`
- その他: `/permissions`, `/introductions`, `/seminar`, `/terms`
- 特殊: `/cable`（WebSocket。UIルートではない）, `*`（404キャッチオール）

> **完全な機械可読ルート一覧は §付録A に82件すべて列挙してある。** 実装の進捗はこのリストにチェックを付けて管理すること。

---

## 5. 難所（高リスクコンポーネント）と対応方針

「触れる完全再現」の工数と失敗の大半はここに集中する。個別に方針を決めておく。

### 5-1. LP / HTMLエディタ（最難関）― `/folders/:.../ab_tests/:.../edit`
- 実態: LPのHTML/CSSを編集するWYSIWYG/コードエディタ。バンドルに `csslint`（unpkg）が読み込まれていた＝**ユーザーがCSSを書ける**。要素ピッカー・プレビュー・保存・バージョン管理を伴う複雑機能。
- 方針:
  - **フェーズ1（今回の"触れる再現"）**: エディタの**ガワ（レイアウト・ツールバー・パネル・プレビュー枠）を再現**し、コードエディタ部分は軽量ライブラリ（例: CodeMirror or Monaco）で"編集できる箱"を置く。保存はモックへPOST（実際には反映されなくてよい/ローカル保持）。
  - WYSIWYGのドラッグ編集・要素ピッカーの完全再現は**フェーズ2以降**（別途見積り）。今回は「開ける・触れる・それらしい」まで。
  - この1機能だけで他ページ数十枚分の重さがある。**独立サブプロジェクトとして切り出す**こと。

### 5-2. ダッシュボードのチャート ― Recharts
- KPIカード群＋折れ線グラフ＋テーブル。
- 方針: クローンでも**Recharts**（または同等のReact SVGチャート）を使い、モックの時系列ダミーで描画。期間切替（今日/昨日/過去3日/過去7日/今週/今月/カスタム）は**Litepicker**でUIを再現し、選択に応じてモックデータを差し替える。

### 5-3. CV速報（リアルタイム）― `/conversions` + ActionCable
- 実態: WebSocket `/cable`（ActionCable）でCV発生をリアルタイム表示。
- 方針: 本番WSには繋がない。**ローカルのモックWSサーバー**（`ws` パッケージ等）を立て、数秒おきに架空CVイベントを push して"流れてくる"体験を再現。ActionCableのメッセージ形式（`{identifier, message}`）に寄せると忠実。

### 5-4. ヒートマップ ― `/ab_tests/:.../heatmaps/comparisons`
- 実態: LP上のクリック/スクロール密度の可視化（比較UI）。
- 方針: LPサムネイル画像＋その上にダミーの密度オーバーレイ（canvas or 半透明レイヤー）を重ねる簡易再現。実データは使わない。

### 5-5. スプリットテスト条件設定（6種）
- devices / oses / carriers / hours / periods / params。各条件でのトラフィック振り分け設定UI。
- 方針: 6ページは**共通コンポーネント（条件テーブル＋トグル＋割合入力）を1つ作って使い回す**（DRY）。差分は選択肢データのみ。

### 5-6. 認証フロー
- Devise系（メール確認 `confirmation`、確認コード `code/new`、パスワード再設定 `forgot_password`/`password/edit`）。
- 方針: **モック認証**。任意の入力でログイン成功扱い→ダッシュボードへ。実際のトークン発行・メール送信はしない。§1-2隔離を守る。

---

## 6. データモデル / モックAPI仕様

クローンの心臓部。**ローカルモックサーバー**が、本番と同じ形のJSONを合成データで返す。

### 6-1. モックサーバー方針
- Node製の軽量サーバー（Express or Vite の dev middleware or `json-server` 拡張）をリポジトリ内 `mock-server/` に置く。
- クローンのフロントは、APIベースURLを環境変数 `VITE_API_BASE=http://localhost:4010`（例）に固定。**本番ドメインは登場させない。**
- レスポンスは `mock-server/fixtures/*.json`（全て架空データ）から返す。ページネーション・ソート・フィルタのクエリにも一応応答する。
- WebSocket（CV速報用）は `mock-server/ws.js` で別ポート。

### 6-2. 主要エンティティ（観測 or ルートから確定）
- **Team**（テナント。マルチテナント構成）
- **User / Member**（`creator:{id,uid,name}` 等で参照される）
- **Folder**（`{id, team_id, uid, name}`）
- **AbTest = beyondページ**
- **Article = 記事**
- **Version**（記事のバリアント。エディタ編集対象）
- **Media = 媒体**（`{id, name, icon_name, ad_cooperation}`）
- RedirectPage / ExitPopup / SplitTestSetting / Heatmap / Report / Conversion / ConversionTag / Form / Task / Inspection / AdAccount / AspAccount / Domain / Tag / Plan / Addon

### 6-3. 採取済みスキーマ（実測。値は型のみ・実データ非採取）

`GET /api/v1/ab_tests`:
```jsonc
{
  "pagination": { "total_pages": "number", "total_count": "number", "current_page": "number" },
  "ab_tests": [
    {
      "id": "number", "team_id": "number", "uid": "string",
      "title": "string", "memo": "string",
      "media_id": "number", "folder_id": "number",
      "published": "boolean", "ad_status": "string", "editor_version": "number",
      "media": { "attributes": { "id": "number", "name": "string", "icon_name": "string", "ad_cooperation": "boolean" } },
      "created_at": "number", "creation_timestamp": "string", "updated_at": "number",
      "folder": { "id": "number", "team_id": "number", "uid": "string", "name": "string" },
      "creator": { "id": "number", "uid": "string", "name": "string" }
    }
  ]
}
```

`GET /api/v1/articles`:
```jsonc
{
  "pagination": { "total_pages": "number", "total_count": "number", "current_page": "number" },
  "articles": [
    {
      "id": "number", "uid": "string", "memo": "string",
      "archived": "boolean", "style_applied": "boolean",
      "created_at": "string", "creation_timestamp": "string", "updated_timestamp": "string",
      "ab_test": { "id": "number", "...": "（要追加採取）" }
    }
  ]
}
```

> 上記2つ以外のエンドポイントのスキーマは、§3の手順で各ページ実装時にNetworkタブから採取して `mock-server/fixtures/` と `docs/api/*.md` に追記していく。

### 6-4. モックが提供すべきエンドポイント（初期リスト。実装で拡充）
- 一覧/詳細: `/ab_tests`, `/ab_tests/:uid`, `/ab_tests/:uid/articles`, `/articles`, `/articles/:uid`, `/folders`, `/folders/:uid`
- ランキング: `/ab_tests/rankings`, `/teams/versions`, `/teams/version-rankings`
- レポート: `/ab_tests/:uid/reports`, `/ab_tests/:uid/reports/lp`, `/ab_tests/:uid/creative_report`, `/conversion-reports`
- 設定系: `/ab_tests/:uid/redirect_pages`, `/ab_tests/:uid/exit_popups`, `/ab_tests/:uid/split_test_settings/*`, `/folders/:uid/conversion_tags`, `/folders/:uid/forms`
- チーム/連携: `/teams/ad_accounts`, `/teams/asp_accounts`, `/teams/domains`, `/teams/tags`, `/teams/plans`
- その他: `/tasks`, `/inspections`, `/report-exclusions`, `/seminar`, `/sb_ai/chat`, `/users/me`, `/addon/option-list`
- リアルタイム: WS `/cable`（ActionCable形式の擬似）

---

## 7. 推奨アーキテクチャ（"HTMLベース・CLI構築"の具体化）

人間の指定「HTMLベース／Claude Code（CLI）で構築」を満たす、推奨構成。**推奨案は1つに絞る（下記）。** 別案が必要なら§12で人間に確認。

### 7-1. 推奨スタック（HTMLファースト・マルチページ）
- **ビルド/実行**: Vite（マルチページ構成）。`npm run dev` で全ページがブラウザで開ける。CLI完結。
- **各ページ = 実HTML**: ルートごとに実体のHTML/テンプレートを持つ（"HTMLベース"の要件を満たす。難読化SPA1枚ではなく、開いて中身が読める）。
- **言語**: TypeScript（型で事故を減らす）。ロジックは素のTS＋小さなヘルパー。重量級フレームワークは必須にしない。
- **スタイル**: 素のCSS（またはCSS Modules）で、§2-4のデザイントークンをCSS変数化。オリジナルのEmotionハッシュは真似ず、**見た目（computed style）を再現**する。
- **難所だけライブラリ可**: チャート=Recharts（この部分はReactの小島にしてよい）／エディタ=CodeMirror or Monaco／日付=Litepicker。**全体をReact化する必要はない。**
- **モック**: §6の `mock-server/`（Node）。フロントとは別プロセス、`npm run mock` で起動。
- **状態**: ページ内は素のJS。共有状態（ログインユーザー・選択チーム）は軽量ストア or localStorage。

> 補足: オリジナルは React+Vite+Emotid のSPAだが、**内部スタックを一致させる必要はない**。要件は「見た目と挙動の忠実な再現」であって「内部実装の一致」ではない。もし将来、Reactの細かい挙動まで一致させたくなったら React+Vite へ切り替える余地を残す（§12の決定事項）。

### 7-2. ディレクトリ構成（多数の小さいファイル。1ファイル200–400行、最大800行）
```
squadbeyond-clone/
├── 企画書.md                  ← 本書
├── docs/
│   ├── routes.md              ← 全82ルート＋進捗チェック（付録A）
│   ├── pages/<route>.md       ← 各ページの採取記録
│   └── api/<endpoint>.md      ← 採取したAPIスキーマ
├── src/
│   ├── shared/                ← レイアウト・サイドバー・ヘッダ・共通UI（ボタン/テーブル/モーダル/タブ/ページネーション）
│   ├── styles/tokens.css      ← §2-4のデザイントークン
│   ├── pages/                 ← ルートごとのページ（フォルダで分類）
│   └── lib/                   ← APIクライアント（localhost固定）・ストア・ヘルパー
├── mock-server/
│   ├── index.ts               ← REST モック
│   ├── ws.ts                  ← CV速報 WS モック
│   └── fixtures/*.json        ← 全て架空データ
└── package.json               ← dev / mock / build / test / lint スクリプト
```

---

## 8. コーディング規約（全社規約の転記。実装AIは遵守必須）

本会話外でも守るべき、御社の全社共通規約を転記する。

- **推測で埋めない**: 仕様が曖昧なら止まって §12 を見て人間に確認（2〜4択＋推奨1つを提示）。存在しないAPI/関数を"ある前提"で書かない。一度決めた選択を勝手に蒸し返さない。
- **機械チェックで証明してから「できた」と言う**: 型（tsc）・lint（eslint）・テスト（vitest等）を実際に走らせ、**緑を確認してから完了報告**。目視で「直った」と言わない。テスト基盤が無ければ手動確認手順を正直に書く。
- **TDD**: バグ修正時は先に"失敗するテスト"を書き、それが緑になったことを直った証拠にする。
- **症状でなく根本原因**: try/catchで握りつぶす・表示側だけ繕う・不要な`if(x==null)return`を"念のため"で足さない。
- **KISS / DRY / YAGNI**: 最もシンプルで実際に動く解を。重複が"現実に"あるときだけ抽出。使う前に抽象化しない。
- **イミュータブル（重要）**: 既存オブジェクトを破壊的変更しない。常に新しいコピーを返す。
- **多数の小さいファイル**: 200–400行が標準、最大800行。機能/ドメインで整理（型別に置かない）。高凝集・低結合。
- **エラーハンドリング**: 全レベルで明示的に。UI面はユーザーに優しいメッセージ、内部は詳細ログ。握りつぶし禁止。
- **入力バリデーション**: 境界（API応答・ユーザー入力・ファイル）で処理前に検証。外部データを信用しない。
- **命名**: 変数/関数=camelCase、真偽=is/has/should/can接頭、型/コンポーネント=PascalCase、定数=UPPER_SNAKE_CASE、フック=use接頭。
- **セキュリティ**: シークレット（APIキー/トークン/パスワード）をソースにハードコードしない（本案件はモックなので本物の秘密は不要）。`.env`／認証情報は絶対にコミットしない。XSS等の基本対策。
- **GitHub**: リポジトリは**private既定**。
- **忠実再現 > 社内UI美観規約**: 「余白ゼロ・エッジtoエッジ」「絵文字禁止→SVGアイコン」「スマホはカード型メニュー」等の社内UI規約は**新規プロダクト向け**。本案件は**オリジナル忠実再現が最優先**なので、オリジナルがそうなっていない限りこれらを適用しない（＝勝手にUIを"改善"しない）。

---

## 9. 受け入れ条件（Acceptance Criteria）

### 9-1. ページ単位（各ルートで満たす）
- [ ] 実サイトと並べて、レイアウト・主要な色・フォント感が一致（§3の照合）。
- [ ] そのページの主要インタラクション（タブ/ソート/ページネーション/モーダル/フォーム）が**モックデータ上で動く**。
- [ ] データは**全て架空**。実在の人名・金額・施策名・ドメインが1つも含まれない。
- [ ] ローディング／空データの見た目がある（分かる範囲で）。
- [ ] `docs/pages/<route>.md` に採取記録と未対応点が書かれている。

### 9-2. 全体
- [ ] サイドバーの全項目から遷移でき、§4のTier1–3が"触れる"状態。Tier4は最低限ガワが出る。
- [ ] `npm run dev` と `npm run mock` の2コマンドで、追加設定なしにローカルで起動する。
- [ ] **本番ドメイン（`*.squadbeyond.com`）へ出ていくネットワークリクエストが0件**（テスト/grepで機械的に保証）。
- [ ] 外部SaaSタグ（Channel.io/Pendo/HubSpot/GTM/Mixpanel/Sentry）を一切ロードしない。
- [ ] `tsc` / `eslint` が緑。主要な共通コンポーネントとモッククライアントに単体テストがある。
- [ ] リポジトリは private。`.env`・認証情報がコミットされていない。

---

## 10. フェーズ / マイルストーン

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| P0. 準備 | リポジトリ初期化、Vite/TS/lint/test雛形、`docs/routes.md`（付録A）配置、デザイントークン `tokens.css` | `npm run dev` で空ページが出る |
| P1. 土台 | 共通レイアウト（サイドバー＋ヘッダ）、モックサーバー雛形、APIクライアント（localhost固定） | 空のシェルが全ルートで表示され404が出ない |
| P2. Tier1 | ダッシュボード（Recharts＋KPI＋一覧）、`/folders`、認証系 | ダッシュボードとフォルダ一覧がダミーで動く |
| P3. Tier2 | beyondページ運用（記事一覧/レポート/スプリット設定/離脱ポップアップ/リダイレクト）、CV速報WS、ヒートマップ簡易、**エディタのガワ** | Tier2が触れる |
| P4. Tier3 | チーム/連携/審査/AI/課金/設定/一括系 | Tier3が触れる |
| P5. Tier4＋仕上げ | 管理者・周辺ページ、全体照合、テスト緑化、受け入れ条件クリア | §9全チェック |
| （将来）P6 | エディタのWYSIWYG完全化、"バグ発見/建築"ツール化 | 別企画で見積り |

---

## 11. リスク一覧

| リスク | 影響 | 対策 |
|---|---|---|
| 実データ混入 | 機密漏洩・信用失墜 | §1-1。合成データ徹底、レビューで実名/実金額の混入をgrep検査 |
| 本番API誤接続 | データ改変・規約違反 | §1-2。localhost固定、本番ドメインgrepテストで0件保証 |
| エディタの底なし工数 | 納期崩壊 | §5-1。今回はガワまで。WYSIWYG完全版は別フェーズ |
| 82ページの物量 | 未完・品質バラつき | §4のTier順＋§3の標準手順で1枚ずつ。共通化(DRY)で量を圧縮 |
| 観測できない挙動（権限分岐・集計ロジック） | "それらしいが不正確" | 擬似挙動で埋め、`docs`に「推測」と明記。忠実さの限界を隠さない |
| ログインセッション切れ | 観測作業が止まる | 実サイト観測は人間ログイン済みのChrome拡張(Claude in Chrome)で実施 |

---

## 12. 未確定事項（実装に入る前に人間へ確認すること）

以下は本企画書では確定していない。実装AIは**勝手に決めず**、着手前に人間へ確認（推奨案を添えて）:

1. **スタック最終確定**: §7の「HTMLファースト・マルチページ（Vite+TS、難所のみライブラリ）」で確定してよいか。それとも本番同様に全面React+Viteにするか。（推奨: §7のHTMLファースト）
2. **エディタの今回到達点**: 「開ける・触れるガワ＋コードエディタの箱」まででよいか（推奨）。WYSIWYG編集の再現をどこまで含めるか。
3. **ランキング（外部 `report.squadbeyond.com`）**: 別ドメインの別アプリ。今回は静的リンク/簡易再現でよいか、そこも全ページ再現対象に含めるか。
4. **モックデータの規模感**: 一覧に並べるダミー件数・媒体の種類（Facebook/LINE/TikTok等の見た目）をどこまで作り込むか。
5. **成果物の置き場所/公開**: private GitHubリポジトリ化するか、ローカルのみか。リポジトリ名。
6. **実サイト観測の分担**: 各ページのデベロッパーツール採取を、人間・Claude in Chrome拡張・実装AIのどれが担うか（拡張はログイン済みChromeが必要）。

---

## 付録A. 全82ルート一覧（機械可読・進捗チェック用）

```
# 認証 / ユーザー
/users/sign_up
/users/confirmation
/users/confirmation/code/new
/users/forgot_password
/users/password/edit
/users/edit
/users/public_api_key
/users/teams

# ダッシュボード
/
/dashboard
/dashboard/:uid

# beyondページ（ab_test）
/ab_tests/:ab_test_uid/articles
/ab_tests/:ab_test_uid/articles/:article_uid/previews
/ab_tests/:ab_test_uid/articles/exit_popups
/ab_tests/:ab_test_uid/articles/htmls/heatmaps/comparisons
/ab_tests/:ab_test_uid/articles/split_test_settings/carriers
/ab_tests/:ab_test_uid/articles/split_test_settings/devices
/ab_tests/:ab_test_uid/articles/split_test_settings/hours
/ab_tests/:ab_test_uid/articles/split_test_settings/oses
/ab_tests/:ab_test_uid/articles/split_test_settings/params
/ab_tests/:ab_test_uid/articles/split_test_settings/periods
/ab_tests/:ab_test_uid/creative_report
/ab_tests/:ab_test_uid/options/devide
/ab_tests/:ab_test_uid/redirect_pages
/ab_tests/:ab_test_uid/reports
/ab_tests/:ab_test_uid/reports/lp
/ab_tests/:ab_test_uid/reports/swipe
/ab_tests/:ab_test_uid/versions/swipe

# フォルダ
/folders
/folders/forms
/folders/:uid/edit
/folders/:folder_uid/ab_tests/:ab_test_uid/edit
/folders/:folder_uid/ab_tests/:ab_test_uid/redirect_pages
/folders/:folder_uid/conversion_tags
/folders/:folder_uid/forms/:uid
/folders/:folder_uid/operator_articles
/folders/:folder_uid/operator_articles/:uid/edit

# 記事一括 / コンバージョン
/articles/bulk_replaces
/conversions
/conversion-reports

# タスク / 審査
/tasks
/inspections
/inspections/authorities
/inspections/folders

# AI
/sb_ai
/sb_ai/chat
/sb_ai/chat/:conversationId

# チーム / 連携
/teams/ad_accounts
/teams/asp_accounts
/teams/domains
/teams/plans
/teams/product_search_forms
/teams/tags
/teams/:team_uid/members/:member_uid/invitations/edit

# 広告OAuthコールバック
/redirections/ab_tests/google_postback_setting
/redirections/ad_accounts/facebook
/redirections/ad_accounts/google
/redirections/ad_accounts/microsoft
/redirections/ad_accounts/x
/redirections/ad_accounts/yahoo

# 設定 / 通知
/settings/internal_notifications/member
/settings/internal_notifications/team
/report-exclusions

# 課金 / アドオン
/plans/:planUid/payment/checkout
/addon/option-list

# 管理者
/admin-report
/admin/articles/html_parts
/admin/plans
/admin/plans/new
/admin/preset_access_denials
/admin/product_search_forms
/admin/teams
/admin/teams/new
/admin/teams/:team_uid/members
/admin/teams/:team_uid/plans/payments

# その他
/permissions
/introductions
/seminar
/terms
/cable        # WebSocket（ActionCable。UIルートではない）
*             # 404 キャッチオール
```

---

## 付録B. 調査の再現方法（実装AIが同じ観測をやり直す手順）

1. Claude in Chrome拡張が入った、**本番にログイン済みのChrome**で `https://app.squadbeyond.com/` を開く。
2. メインJSバンドルURL（`/assets/index-<hash>.js`）を取得し、`fetch`して `path:"..."` 文字列を正規表現抽出 → 全ルートが得られる（本企画書はこの方法で82件を抽出）。
3. 各ページに遷移し、Networkタブで叩いているエンドポイントとレスポンス構造を採取。**値ではなく構造（フィールド名/型）を採る。**
4. computed style から色・余白・フォントを採取（Emotionのクラス名は無視）。
5. 採取内容を `docs/` に記録し、§3の手順で再現する。

（本文書の技術情報はすべて 2026-08-31 時点の実測。オリジナルの更新で変わりうる。）
